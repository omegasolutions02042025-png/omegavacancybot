import google.generativeai as genai
import asyncio
import json
import re
from datetime import datetime
import os
from dotenv import load_dotenv

load_dotenv()


GEMINI_API_KEY = os.getenv('GEMINI_API_KEY')

genai.configure(api_key=GEMINI_API_KEY)


async def process_vacancy_with_gemini(text: str) -> dict | None:
    """
    Анализирует текст вакансии с помощью Gemini API, выполняет фильтрацию,
    очистку и извлечение данных в один шаг.

    Args:
        text: Строка с полным текстом вакансии.

    Returns:
        Словарь (dict) с извлеченными данными в формате JSON
        или None, если вакансия была отфильтрована.
    """
    print("🚀 Запускаем обработку вакансии через Gemini...")

    # Текущая дата для prompt
    current_date = datetime.now().strftime("%d.%m.%Y")
    current_time = datetime.now().strftime("%H:%M")
    current_tz = "+03:00"  # МСК по умолчанию

    # --- ЕДИНЫЙ ПРОМПТ ДЛЯ GEMINI ---
    system_prompt = """
    Ты — фильтр вакансий.
            На вход тебе дается текст вакансии. Твоя задача — проанализировать его, отфильтровать и извлечь данные в формате JSON.

            ### Этап 1: Фильтрация
            Если вакансия не проходит хотя бы по одному из правил ниже → верни null.
            
            Продолжительность: 
            
            Отсечь, если продолжительность или срок проекта менее 3 месяцев. 
            Проекты должны быть от 3 месяцев и более. 
            Примеры для отсева: "1 месяц", "2 мес", "до 2 месяцев", "менее 3 месяцев", "2 месяца", "от 2 месяцев" и т.п. 
            Примеры для пропуска: "3 мес", "от 3 мес", "3+ мес", "4 мес", "от 6 мес", "6+ мес" и т.п. 
            Если написано "от 3х месяцев +" — НЕ отсекать (это подходит).
            
            
            Дедлайн:
            
            Отсечь, если дедлайн в прошлом.
            
            Если в вакансии не указано не прожолжительность проекта ни дедлайа — то utochnenie = true.

            Локация и гражданство (единое правило):

            ❌ Вакансия должна быть отклонена, если выполняется хотя бы одно из условий:
            - Локация ограничена только "РФ" или "Россия" (и нет упоминания РБ).
            - Локация — список стран, где отсутствует РБ.
            - Локация содержит "кроме РБ".
            - Гражданство ограничено только "РФ" или списком стран без РБ.
            - Гражданство содержит "кроме РБ".

            ✅ Вакансия проходит, если одновременно:
            - Локация подходит (есть РБ, "РФ и РБ", "Дружественные страны", "Любые", "Без ограничений", "РФ (предпочтительно)").
            - Гражданство подходит (есть РБ, "РФ и РБ", "Дружественные страны", "Любые", "Без ограничений", "Без разницы").

            Если в тексте есть слово 'stop' или 'стоп' → верни null.

            ### Этап 2: Извлечение данных и очистка (если вакансия прошла фильтрацию)

            **1. Контакты:**
            * Найди блок "Вопросы и предложения ➡️@username...". Замени его на "Вопросы и предложения пишите Дмитрию ➡️@Dimitryver. Указать 🆔 запроса.".
            * Удали все остальные контакты: Telegram (@...), телефоны, email, ссылки.
            * **ОБЯЗАТЕЛЬНО СОХРАНИТЬ информацию об оформлении (например, "Оформление:", "Оформление в РЛ на минимальную ставку" и т.п.) в тексте.**
            **2. Заголовок (`vacancy_title`):**
            2. Заголовок (`vacancy_title`):
          * Если в начале есть строка-заголовок (например, "🥇 Java разработчик..."), удали её из текста и верни в ключ "vacancy_title" (с сохранением всех символов, включая эмодзи и пробелы). Если нет — null.

            **3. ID Вакансии (`vacancy_id`):**  
            * Найди первый ID формата **"🆔 XX-1234", "🆔 8581", "XX-1234", "8581", "1с 8474" или "1с 1234"**.. Удали его из текста и верни значение в ключ "vacancy_id" без символа 🆔. Удали его из текста и верни значение в ключ "vacancy_id" (без "🆔"). Если нет — null.

            **4. Ставка (`rate`):**
            * Найди ставку. Если есть ставка для РФ и не для РФ, бери для не РФ. Верни только число. Если ставки нет — верни 0.
            * Если есть фраза вида **"Отсрочка платежа ..."**, то верни её содержимое в переменной `delay_payment`, удаляя слова "Отсрочка платежа", кавычки и лишние пробелы.  
            Примеры:
            - "Отсрочка платежа 35 рабочих дней" → "35 рабочих дней"
            - "Отсрочка платежа: 45 р.д." → "45 р.д."
            - "Отсрочка платежа - 50 календарных дней" → "50 календарных дней"
            * Полностью удали из текста все строки или фрагменты строк, где упоминаются:
            - "Ставка закупки"
            - "Ставка"
            - "Условия оплаты"
            - "Оплата"
            - "Отсрочка"
            - "Актирование"
            - числа в днях (например: "35 р.д.", "50 календарных дней", "45 рабочих дней")
            * Эти строки или их части нужно удалить полностью.
            
            * ❗️ При этом обязательно сохранить всё, что связано с "Оформление" (например, "Оформление:", "Оформление в РЛ на минимальную ставку").


            **5. Дедлайн ('deadline_date', 'deadline_time'):**
            * Найди дедлайн "до DD.MM.YYYY HH:MM". Извлеки дату и время в соответствующие ключи. Если их нет — null.
            
             **6. Актирование (`acts`):**
            * Если в тексте встречаются фразы:
              - "акты поквартально"
              - "квартальное актирование"
              - "поквартальная оплата"
              - "актирование"
              - "поквартально"
              - "квартальная оплата"
              - "поквартальная"
            то в ключ `acts` верни **true** или **false**.
            
            **7. Продолжительность проекта (`short_project`):**
            * Если в тексте указана продолжительность проекта менее 6 месяцев (например: "3 месяца", "4 мес.", "полгода", "до 5 месяцев"), верни `short_project: true`.
            * Если срок 6 месяцев или больше, либо не указан, верни `short_project: false`.
            
            **8. Только штатные кандидаты (`only_fulltime`):**
            * Если в тексте встречаются фразы:
              - "Только штатные кандидаты"
              - "Рассматриваем только штатных кандидатов"
              - "Оформление только штат компании"
            то верни `only_fulltime: **true**`, иначе — `only_fulltime: **false**`.
            
            **9. Удаление строк с заголовками (`plus`):**
            1.  **Найди строки**, которые полностью или частично соответствуют одному из следующих заголовков (могут содержать двоеточие или эмодзи ➕):
                * `Будет плюсом`
                * `Плюсом будет`
                * `Дополнительно`
                * `Доп. требования`
                * `Желательно`
                * `Приветствуется`

            2.  **Действие:** Удали **только** найденную строку с заголовком. Не трогай, не перемещай и не удаляй текст, который находится под этим заголовком.

            3.  **ВАЖНОЕ УСЛОВИЕ:** Если в тексте нет ни одного из этих заголовков, верни исходный текст без каких-либо изменений. Сохраняй все переносы строк и абзацы.
            
            
            **10. Сохранение форматирования:**
            * ❗️ **КЛЮЧЕВОЕ ПРАВИЛО:** В итоговом поле "text" сохрани абсолютно все исходные переносы строк, абзацы и пустые строки, за исключением удаленных блоков. Не меняй порядок строк.
            
            ### Этап 3: Формат ответа
            Ответь строго в JSON, без текста до или после.
            Формат (все поля обязательны, даже если пустые):

            {
              "text": "<очищенный текст вакансии с сохраненными абзацами>",
              "rate": "<число или 0>",
              "deadline_date": "DD.MM.YYYY" или null,
              "deadline_time": "HH:MM" или null,
              "vacancy_id": "<id или null>",
              "vacancy_title": "<заголовок или null>"
              "utochnenie": true или false,
              "delay_payment": "<фраза или null>",
              "acts": true или false,
              "short_project": true или false,
              "only_fulltime": true или false
              "utochnenie": true или false
            }
    """

    # Модель Gemini
    model = genai.GenerativeModel('gemini-2.5-flash')
    generation_config = genai.types.GenerationConfig(temperature=0.1)

    full_prompt = system_prompt + "\n\nТекст вакансии:\n\n" + text

    try:
        response = await model.generate_content_async(
            full_prompt,
            generation_config=generation_config
        )

        clean_text = response.text.strip()
        print("✅ Ответ от Gemini получен.")
        print(clean_text)

        # Проверка на отсечение вакансии
        if clean_text.lower() == 'null':
            print("❌ Вакансия отфильтрована.")
            return None

        # Попытка извлечения JSON
        json_match = re.search(r'\{.*\}', clean_text, re.DOTALL)
        if json_match:
            try:
                return json.loads(json_match.group(0))
            except json.JSONDecodeError as e:
                print(f"Ошибка парсинга JSON: {e}")
                return None
        else:
            print("JSON не найден в ответе модели.")
            return None

    except Exception as e:
        print(f"Ошибка при вызове Gemini API: {e}")
        return None


async def format_vacancy_gemini(text: str, vacancy_id: str, date: str | None = None) -> str:
    """
    Функция для структурированного оформления вакансии с помощью Gemini API.
    Возвращает готовый текст в нужном формате.
    """
    if date is None:
        date = datetime.now().strftime("%d.%m.%Y")
        
    print(f"[*] Форматируем вакансию ID: {vacancy_id} от {date}...")
    
    # Промпт для Gemini, объединяющий системную и пользовательскую роли
    prompt = f"""
Ты — редактор вакансий.
На вход ты получаешь текст вакансии. Твоя задача — оформить его в структурированном виде для публикации, строго соблюдая формат.

⚡️ ВАЖНО:
— Никогда не удаляй и не изменяй HTML-тэги (<s>, <b>, <i> и любые другие).
— Особенно не трогай тэги вокруг следующих строк:
  * «ИП: ...»
  * «Самозанятый: ...»
  * «- Ежемесячная выплата Штат/Контракт»
— Эти элементы должны оставаться с тэгами в исходном виде.

⚡️ Правила форматирования:
— В названии должности всегда указывать грейд (Junior / Middle / Senior / Lead), если он есть.
— Раздел «Будет плюсом» включается в общий список «💻 Требования» (без отдельного блока).
— Каждый пункт требований — отдельной строкой с маркером.
— Между разделами и блоками вакансии должна быть ровно 1 пустая строка.
— В тексте вакансии не должно быть 2 и более пустых строк подряд.
— Не удаляй и не сокращай информацию, используй все данные из текста.
— Блок «⚠️ Особые условия» включай только если в тексте реально есть данные (NDA, кол-во этапов интервью, оформление и т.д.).

⚡️ Структура итогового текста:

🆔{vacancy_id}
📅 Дата публикации: {date}

🥇 [Название должности] ([Грейд])

💰 Месячная ставка (на руки) до:
- Ежемесячная выплата Штат/Контракт: [сумма] RUB
- С отсрочкой платежа "[срок, например: 35 рабочих дней]" после подписания акта ([Актирование ежемесячно/поквартальное]):
  ИП: [сумма] RUB,
  Самозанятый: [сумма] RUB

📍 Локация/Гражданство: [страны, города]
🏠 Формат работы: [удалённо / офис / гибрид]
🎓 Грейд: [Junior / Middle / Senior / Lead]
📆 Срок проекта: [сроки]
🚀 Старт проекта: [ASAP или дата]

📌 О проекте:
[Краткое описание проекта]

📎 Задачи:
— [Перечислить задачи из вакансии]

💻 Требования:
— [Обязательное требование 1]
— [Обязательное требование 2]
— [Будет плюсом 1]
— [Будет плюсом 2]

⚠️ Особые условия:
— [NDA, кол-во этапов интервью, трудоустройство и т.д.]

❗️ Обязательные данные по кандидату при подаче:
● ФИО
● Страна + Город
● Дата рождения (не возраст, а дата)
● Электронная почта
● Образование (ВУЗ, год окончания, специальность)
● Грейд
● Ставка
● Чек-лист соответствия требованиям (ДА/НЕТ)

Контакт для вопросов: [имя + @Telegram]

---
Вот текст вакансии для обработки:

{text}
"""
    
    model = genai.GenerativeModel('gemini-2.5-flash')
    generation_config = genai.types.GenerationConfig(temperature=0.1)
    
    try:
        response = await model.generate_content_async(prompt, generation_config=generation_config)
        return response.text.strip()
    except Exception as e:
        print(f"🔥 Ошибка при форматировании вакансии: {e}")
        return False


async def generate_hashtags_gemini(vacancy_text: str) -> str:
    """
    Генерирует хэштеги из текста вакансии с помощью Gemini API.
    """
    print("[*] Генерируем хэштеги...")
    
    prompt = f"""
Вот текст вакансии:

{vacancy_text}

Сформируй список хэштегов в одну строку через пробел.

Правила:
1. По технологиям/стеку (например: #Java #Spring #PostgreSQL #Docker) — извлеки их из описания.
2. По грейду: #Junior или #Middle или #Senior — определи из текста.
3. По отрасли (например: #Fintech #Retail #Banking) — определи по контексту.

Выведи только строку с хэштегами.
"""
    
    model = genai.GenerativeModel('gemini-2.5-flash')
    generation_config = genai.types.GenerationConfig(temperature=0.1)
    
    try:
        response = await model.generate_content_async(prompt, generation_config=generation_config)
        return response.text.strip()
    except Exception as e:
        print(f"🔥 Ошибка при генерации хэштегов: {e}")
        return False



# --- ПРИМЕР ИСПОЛЬЗОВАНИЯ ---
async def main():
    # Пример текста вакансии для тестирования
    sample_vacancy_text = """
🆔8748


🥇 UX исследователь

Грейд: senior
Локация специалиста: Россия, РБ
Тайм-зона проекта: мск
Гражданство: РФ

Описание проекта: Проект для крупного банка. Команда развития мобильных точек продаж в банке. Зона ответственности - ключевые каналы, через которые клиент узнает и покупает продукты в мобильном банке: - баннеры, сторис, поп-апы, чат, шторки; - раздел «Витрина» - место, где клиент открывает банковские продукты. Миссия - сделать эти точки продаж максимально удобными, персонализированными и результативными. Необходимо, чтобы клиент за несколько кликов находил нужный продукт, а бизнес получал максимальный эффект от цифровых каналов. В команде есть продукт-менеджеры, дизайнеры, аналитики и разработчики. Тесно работают с другими командами банка и постоянно проверяют гипотезы, тестируют новые механики и улучшают клиентский путь. Чтобы разрабатывать качественный и востребованный функционал в мобильном приложении, нужно глубоко понимать задачи, мотивы и барьеры клиентов. Для этого нужен исследователь, который: - обеспечит команду достоверными данными о клиентском опыте; - поможет проверять гипотезы до начала разработки, чтобы минимизировать риск лишних итераций и затрат; - выявит реальные «работы» (Jobs To Be Done) клиентов и барьеры, мешающие им достигать целей; - понимает теорию качественных и количественных исследований, понимает процесс и ценности каждого вида исследований; - превратит инсайты в конкретные рекомендации по функционалу и UX/UI. Результаты работы исследователя будут напрямую использоваться при планировании и приоритизации задач разработки. В планах - редизайн раздела «Витрина». Исследователь должен провести JTBD-интервью, изучить сценарии поведения клиентов, выявить барьеры, протестировать прототипы и сформировать рекомендации, которые станут основой для проектирования и разработки нового дизайна.
Название конечного клиента или отрасль: Банки и финансы
Дата старта проекта: ASAP 
Продолжительность проекта (от 3х месяцев): до конца года (с возможностью продления)

Оформление: фулл-тайм
Ставка закупки:  1720 - 2150 (клиент ориентируется на низкую ставку)
Загрузка: фулл-тайм
Условия оплаты: отсрочка 35 рабочих дней 


Вопросы и предложения Екатерина Пантюхина ➡️@katushar или в общий чат. 
Указать 🆔 запроса. 

❗️Обязательные данные по кандидату при подаче: ❗️
● ФИО
● Дата рождения
● Страна+Город
● Грейд
● Ставка
● Оценить требования ДА/НЕТ, в соответствии с наличием опыта.️
● ВСЕ ТРЕБОВАНИЯ ИЗ ЗАПРОСА ОТРАЖЕНЫ В ПРОЕКТАХ РЕЗЮМЕ


📌 Задачи:
- Проводить качественные исследования (интервью, юзабилити-тесты, фокус-группы) для понимания задач и барьеров клиентов; 
- Проводить количественные исследования (опросы, анализ метрик, A/B-тесты) для подтверждения гипотез; 
- Применять JTBD, CJM, UX/UI исследования для поиска и тестирования идей; 
- Анализировать поведение пользователей в мобильном приложении и выявлять точки роста; 
- Формировать отчеты и рекомендации, которые будут использованы в разработке; 
- Работать с продукт-менеджерами, дизайнерами и аналитиками для интеграции результатов исследований в продукт; 
- Развивать исследовательскую культуру в команде.

💻 Требования: 
- Опыт проведения исследований полного цикла - от постановки задачи до внедрения результатов в продукт; 
- Владение JTBD, CJM, usability-тестированием, интервью, опросами и количественным анализом; 
- Понимание специфики мобильных продуктов и цифровых каналов продаж; 
- Опыт работы с метриками (NPS, CSAT, конверсия, retention); 
- Умение формулировать выводы и рекомендации так, чтобы они были понятны и полезны разработке.
- Опыт в финтехе, e-commerce или в банках; 
- Опыт проведения A/B-тестов; 
- Опыт взаимодействия с командами разработки и UX-дизайна.

❗️ В резюме в каждом проекте должны быть данные - период, роль, задачи/обязанности, стек применяемых технологий, отрасль проекта.
    """
    
    # Вызов основной функции
    result = await process_vacancy_with_gemini(sample_vacancy_text)
    
    # Красивый вывод результата
    if result:
        print("\n--- ИТОГОВЫЙ РЕЗУЛЬТАТ ---")
        print(json.dumps(result, indent=2, ensure_ascii=False))
    else:
        print("\n--- ОБРАБОТКА ЗАВЕРШЕНА ---")
        print(json.dumps(result, indent=2, ensure_ascii=False))


if __name__ == "__main__":
    # Запуск асинхронной функции
    asyncio.run(main())