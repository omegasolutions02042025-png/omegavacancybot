import google.generativeai as genai
import asyncio
import json
import re
from datetime import datetime
import os
from dotenv import load_dotenv

load_dotenv()

GEMINI_API_KEY = os.getenv('GEMINI_API_KEY')
genai.configure(api_key=GEMINI_API_KEY)


async def process_vacancy_with_gemini(text: str) -> dict | None:
    """
    Анализирует текст вакансии с помощью Gemini API, выполняет фильтрацию,
    очистку и извлечение данных в один шаг.

    Args:
        text: Строка с полным текстом вакансии.

    Returns:
        Словарь (dict) с извлеченными данными в формате JSON
        или None, если вакансия была отфильтрована.
    """
    print("🚀 Запускаем обработку вакансии через Gemini...")

    # Текущая дата для prompt
    current_date = datetime.now().strftime("%d.%m.%Y")
    current_time = datetime.now().strftime("%H:%M")
    current_tz = "+03:00"  # МСК по умолчанию

    # --- ЕДИНЫЙ ПРОМПТ ДЛЯ GEMINI ---
    system_prompt = f"""
    Ты — фильтр вакансий. На вход приходит ТЕКСТ ВАКАНСИИ. Работай строго по правилам ниже.
При противоречиях выбирай ОТСЕЧЬ. Логика между разделами — И (AND).
Сегодня: {current_date} {current_time} ({current_tz}).

ЦЕЛЬ:
1) Решить, отсекать вакансию или нет (если отсечь — вернуть структуру с полями = null и в переменной "reason" указать причину).
2) Если НЕ отсекать — очистить и нормализовать текст, извлечь поля и вернуть словарь строго заданного формата.

ГЛОБАЛЬНОЕ ПРАВИЛО СОХРАНЕНИЯ СТРУКТУРЫ (ВАЖНО):
— По всему тексту вакансии СТРОГО СОХРАНЯЙ исходную структуру: порядок строк и абзацев, пустые строки,
  маркеры списков (-, •, —), нумерацию (1), 1., 1), тире/кавычки, эмодзи и спецсимволы, регистр,
  последовательности пробелов/табов. Ничего не переформатируй и не «склеивай».
  Разрешено удалять/перемещать ТОЛЬКО явно указанные ниже фрагменты (оплата/контакты/заголовки «плюсов»/🆔 и т.п.).
  Все прочие символы и переносы должны остаться без изменений.

НОРМАЛИЗАЦИИ:
НОРМАЛИЗАЦИИ:
- Определи страну/локацию по ключевым словам.
- Если в тексте встречаются слова «РФ», «Россия», «Moscow», «Москва», «Санкт-Петербург», «Казань», «Новосибирск» и т.п. — добавь "РФ" в список location.
- Если встречаются слова «Беларусь», «BY», «Минск», «Гродно», «Брест», «Витебск», «Гомель», «Могилёв», «дружественные страны», «СНГ», «CIS», «EAEU», «Eurasian», «Kazakhstan», «Казахстан», «Kyrgyzstan», «Киргизия», «Armenia», «Армения», «Azerbaijan», «Азербайджан» — добавь "РБ" в список location.
РБ-группа/«дружественные/СНГ/EAEU/CIS/Eurasian/Kazakhstan/Казахстан/Kyrgyzstan/Кыргызстан/Armenia/Армения/Azerbaijan/Азербайджан» если есть такие фразы или похожие то добавь "РБ" в список location.
- Если указано «удалённо», «remote», «any location», «гибрид», «работа из любой точки», добавь обе ("РФ" и "РБ") в список location.
- Если указаны обе группы (РФ и РБ/СНГ) — добавь обе страны: ["РФ", "РБ"].
- Если указано несколько локаций — возвращай список (например ["РФ", "РБ"]).
- Если не удаётся определить — верни пустой список [].
- Тайм-зоны: МСК/MSK = UTC+3. Если TZ не указана, используй {current_tz}.
- Нормализация длительности: «3х/3-х/3 x» трактуй как «3».

БЛОК A. СРОКИ ПРОЕКТА (ПРОДОЛЖИТЕЛЬНОСТЬ И ДЕДЛАЙН)
A1. ПРОДОЛЖИТЕЛЬНОСТЬ (< 3 месяцев → ОТСЕЧЬ):
— ОТСЕЧЬ, если длительность < 3 месяцев (в т.ч. «1/2 мес», «до 2 мес», «≈2–2.5 мес», «3 мес-» и т.п.).
— ОТСЕЧЬ, если длительность НЕ УКАЗАНА (есть «Продолжительность:» без значения в этой строке и в следующей).
— «от N до M»: смотри нижнюю границу для отсева (<3 → отсев). Для флага «короткий проект» смотри верхнюю границу (см. L).
— «до конца года»: если от текущей даты до конца года < 90 дней → считать < 3 месяцев → ОТСЕЧЬ.
— НЕ отсекать, если явно ≥ 3 месяцев: «3 мес», «от 3 мес», «3–6 мес», «≥3», «6+ мес», «полгода», «год», «long-term» и т.п.

A2. ДЕДЛАЙН (в прошлом или истёк к текущему моменту → ОТСЕЧЬ):
  A2.1. Распознавай даты/время/TZ: DD.MM.YYYY, DD.MM.YY, DD/MM/YYYY, YYYY-MM-DD, «30 сентября 2025», «до 30.09»,
        а также «сегодня», «завтра», «до конца дня» (EOD/COB), EOW/EOM, «до пятницы», «до 18:00 МСК», «до 23:59», «до 15:00 UTC+3».
  A2.2. Если указан часовой пояс (МСК/MSK/UTC±X) — используй его, иначе {current_tz}.
  A2.3. Если год не указан — используй текущий год. Если «до [месяц]» без дня — дедлайн = последний день месяца 23:59 (в соответствующей TZ).
  A2.4. Отсекай, если:
        • дедлайн < текущего момента; ИЛИ
        • дедлайн = сегодня и время НЕ указано; ИЛИ
        • дедлайн = сегодня и указано время ≤ текущего времени.
        (Если дедлайн сегодня и время > текущего — НЕ отсекать.)
  A2.5. ШАПКА БЕЗ ЗНАЧЕНИЯ — НЕ ОТСЕКАТЬ: если есть только заголовок/лейбл дедлайна (строка оканчивается «:»)
        и нет даты/времени ни в этой строке, ни в непосредственной следующей строке — A2 не применяется.
  A2.6. «ASAP/как можно скорее» — это не дедлайн (A2 не применяется).
  A2.7. Если дедлайн не найден — A2 не применяется.
  A2.8. Распознавай форматы: DD.MM.YYYY, DD.MM.YY, DD/MM/YYYY, YYYY-MM-DD, «30 сентября 2025», «до 30.09», «сегодня», «завтра», «до конца дня» (EOD/COB), EOW/EOM, «до пятницы», «до 18:00 МСК», «до 23:59», «до 15:00 UTC+3».
  Если год не указан — подставь текущий. Если «до [месяц]» без дня — последний день месяца 23:59 (в указанной TZ).
  «сегодня» без времени: внутренне считай 18:00 локального времени проверки. Если сейчас ≥ 18:00 — ОТСЕЧЬ, иначе — нет. В ответ время не подставляй.
  ОТСЕЧЬ, если дедлайн в прошлом; либо «сегодня» и (по правилу) уже истёк.
 «ASAP/как можно скорее» — не дедлайн (не влияет на отсев).

1) Жёсткая гарантия сохранения структуры (усиль самый верх промпта)

Вместо общего «сохраняй структуру» вставь:

ИНВАРИАНТ СТРУКТУРЫ (ЖЁСТКО):
— Запрещено удалять, сокращать, перефразировать или объединять строки за пределами явно разрешённых случаев.
— Разрешено удаление ТОЛЬКО:

упоминаний оплаты/актирования/отсрочки (см. блок “Удаление оплаты”),

строки заголовка вакансии (первая строка-заголовок) — но только эту строку,

ФИО контактного лица в строке «Вопросы и предложения …» — удаляется только ФИО, остальное в строке сохраняется.
— Запрещено удалять или сокращать любые другие разделы: «Особые условия», «Требования», «Задачи», «О проекте», «Мы предлагаем», «Примечания», «!!! …», и т. п.
— Если есть сомнение — ничего не удаляй.

2) Белый список разделов, которые НЕЛЬЗЯ трогать

Добавь отдельный пункт:

ЗАПРЕТ НА МОДИФИКАЦИИ СЕКЦИЙ:
Разделы «Особые условия», «⚠️ Особые условия», «!!! …», «Примечания», «Важно», «Notes», «Important», «Responsibilities/What you will do», «Requirements», «Nice to have» — никогда не удалять, не сокращать, не переносить. Сохраняй их полностью, включая эмодзи, пунктуацию и пустые строки.

3) Безопасное удаление оплаты (сузь формулировку, чтобы не “захватывало” следующее)

Замени существующий пункт про оплату на:

Удаление оплаты (безопасное):
Удаляй только строки, которые явно относятся к оплате/актированию/отсрочке/ставкам. Это отдельные строки, начинающиеся (или содержащие маркеры) типа:
— 💰, Ставка:, Оплата:, Условия оплаты:, Актирование:, Отсрочка платежа, net 30/35/45/50/60/90, N30/35/…, оплата 35РД, оплата 50 к.д. и т. п.
Удаляй строку целиком, но только её. Не удаляй последующие абзацы.
НЕЛЬЗЯ удалять любой текст после пустой строки или заголовка следующего раздела.
Если строка неоднозначна — оставь её (ничего не удаляй).

(Смысл: запрещаем «жадное» удаление, которое часто съедает «Особые условия» ниже.)

4) Контакты — удалять только ФИО, оставлять остальное

Уточни правило Q:

Контакты (Q):
В строке вида «Вопросы и предложения {{ФИО}}   ➡️@username …» — удали только ФИО.
Оставь «Вопросы и предложения», @username, «или в общий чат», ID и прочее — всё без изменений.
Пример:
Вход: Вопросы и предложения Сазанович Михаил ➡️@sazanovich_ma или в общий чат.
Выход: Вопросы и предложения ➡️@sazanovich_ma или в общий чат.

БЛОК B. ОБЩИЕ КРИТЕРИИ ОТСЕВА (любой пункт → ОТСЕЧЬ):
B1. Запрет на подачу: «стоп»/«STOP»/«❌ стоп» и подобные.

БЛОК С. ВЫВОД ДОПОЛНИТЕЛЬНОЙ ИНФОРМАЦИИ:
С1. Выводи причину отсева в переменной reason.
С2. Если нет локации или гражданства — верни utochnenie - true

ЕСЛИ выполнено хотя бы одно условие отсева из блоков A или B,
— верни структуру с полями = None (см. «ФОРМАТ ОТВЕТА»). Иначе — продолжай обработку.

ОБРАБОТКА ТЕКСТА (выполняй последовательно; применяется ТОЛЬКО если вакансия НЕ отсечена):
A) УДАЛЕНИЕ УПОМИНАНИЙ ОПЛАТЫ: полностью удали любые условия оплаты/актирования/сроки оплаты/отсрочки.
B) ЗАГОЛОВОК ВАКАНСИИ: если в начале есть заголовок (например: «🥇 Java разработчик c Camunda»),
   УДАЛИ его из текста и верни в "vacancy_title" (без эмодзи и лишних пробелов); остальной текст не меняй.
C) ИДЕНТИФИКАТОР: найди ПЕРВЫЙ идентификатор:
   • «🆔 XX-1234» (XX — латинские заглавные, 1234 — цифры) ИЛИ
   • «🆔 8581» (только цифры).
   УДАЛИ его из текста и верни "vacancy_id" (без символа 🆔). Если нет — None.
D) СТАВКА
  • Базовая извлекаемая ставка ("rate"):
  • Если найдено хотя бы одно число (одиночное или диапазон), сформируй "rate" как объект по юрисдикциям.
  • Если единственное число без пометок и витрина допускает РФ+РБ → рассчитай:
      "rate" = {{"РФ": amount_RF, "РБ": floor(amount_RF*0.83)}}
  • Если диапазон «N–M» без пометок и витрина допускает РФ+РБ → рассчитай по нижней границе:
      "rate" = {{"РФ": N, "РБ": floor(N*0.83)}}
  • Если даны две ставки «РФ» и «не РФ» → "rate" = {{"РФ": amount_RF, "РБ": amount_non_RF}}
  • Если указано только «для РФ» (и витрина допускает РБ) → "rate" = {{"РФ": amount_RF, "РБ": floor(amount_RF*0.83)}}
  • Если указано только «для не РФ» (и витрина допускает РФ) → "rate" = {{"РФ": null, "РБ": amount_non_RF}}
  • Если витрина допускает только одну юрисдикцию (например, только РФ) — "rate" выводи только с этой юрисдикцией (вторая = null).
  • Если ставки НЕТ → "rate"=0
E) ТРЕБОВАНИЯ (объединение «плюсов» с основными):
   G1. Основной список — разделы «Требования», «Основные требования», «Обязательные требования»,
       англ.: «Requirements», «Required», «Must have». Если их нет, но есть «плюсы» — создай раздел «Требования»
       и помести его на место первого найденного блока требований/«плюсов».
   G2. Что считать «плюсами» (без учета регистра/пунктуации; кавычки/маркеры НЕ обязательны):
       — Заголовки/подзаголовки/фразы: «будет плюсом», «будет большим плюсом», «будет преимуществом»,
         «большим преимуществом будет», «плюсом», «плюсом будет», «как плюс»,
         «желательно», «предпочтительно», «не обязательно, но желательно», «опционально», «Доп. требования:»;
         англ.: «nice to have», «good to have», «would be a plus», «big/strong plus», «preferred»,
         «optional», «bonus», «as an advantage», «advantageous».
       — Фразы-связки с «если»: распознавай конструкции «будет плюсом(,|—|-|:)?\s*если …»
         (в т.ч. «Будет плюсом если вы …») — с любыми знаками препинания или без — и относись к ним как к «плюсам».
       — Эти фразы могут встречаться в заголовке раздела, внутри пункта, в начале/конце предложения,
         в скобках («…(будет плюсом)»), с эмодзи/символами (напр. «➕», «+», «🌟», «⭐») или без них — во всех случаях
         считай такой пункт «плюсом».
   G3. Что НЕ считать «плюсами»: разделы про условия/бонусы/преимущества работодателя
       («Преимущества», «Бонусы», «Мы предлагаем», «Benefits», «Perks», «Conditions», «Условия»), а также
       «Задачи/Обязанности/Responsibilities/What you will do».
   G4. Как объединять:
       — Вырежи ТОЛЬКО заголовки «плюсов»/вводные слова, сами пункты оставь как есть.
       — Если «плюсы» перечислены одной строкой через запятые — перенеси строку целиком как один пункт,
         не меняя пунктуации.
       — Если пометка «(будет плюсом/optional/…)» стоит в конце пункта, перенеси пункт в конец списка
         требований и УДАЛИ только скобочную пометку.
       — Порядок: 1) исходные обязательные пункты (как были), 2) затем все «плюсы» в порядке появления сверху вниз.
       — Дубли: убери точные дубликаты (trim + без финальной точки/«;»). Семантические различия — не объединяй.
H) ДЕДЛАЙН (извлечение значений, НЕ отсев здесь): определи срок подачи резюме ().
   Верни  Дату  (до какого числа "deadline_date" = "DD.MM.YYYY" или None) и Время (до какого времени "deadline_time" = "HH:MM" или None).
I) СТРУКТУРА (напоминание): после всех манипуляций глобально соблюдай правило сохранения структуры текста
   (см. блок «ГЛОБАЛЬНОЕ ПРАВИЛО СОХРАНЕНИЯ СТРУКТУРЫ»).
K) Если встречаются фразы/эквиваленты (любой регистр/язык):
    «акты поквартально», «квартальное актирование», «поквартальная оплата», «актирование», «ежеквартально»,
    «quarterly invoicing/billing», «net 60», «net 90», «N60», «N90», «deferred payment», «post-payment».
    Верни "acts" = true. В противном случае — false.

L) Если в тексте указана длительность проекта менее 6 месяцев (например: "3 месяца", "4 мес.", "полгода", "до 5 месяцев", "3 мес+"), то возвращай short_project: true.
    Если указано "от N месяцев", то это нижняя граница, а не ограничение сверху. В этом случае не считай проект коротким, даже если N < 6.
    Если указано "от N до M месяцев", смотри на верхнюю границу: если M < 6, то short_project: true, иначе false.
    Если указано "до конца года", "с возможностью продления", или другая формулировка, явно предполагающая срок ≥ 6 месяцев, то short_project: false.
    В остальных случаях — short_project: false.

M) Если встречаются фразы вида:

    - "Отсрочка платежа ..."
    - "Отсрочка ..."
    - "Условия оплаты: отсрочка ..."
то верни содержимое после слова "отсрочка" в переменной delay_payment, удаляя само слово и лишние пробелы. Иначе — delay_payment: null.

N) Если в отсрочка платежа больше 35 рабочих дней или 50 календарных дней, верни long_payment: true, иначе — long_payment: false.


O) Если в тексте встречаются фразы:
              - "Только штатные кандидаты"
              - "Рассматриваем только штатных кандидатов"
              - "Оформление только штат компании"
            то верни only_fulltime: true, иначе — only_fulltime: false.

P) Оставь в тексте срок подачи резюме

Q) УДАЛИ ИЗ ТЕКСТА ИМЯ И ФАМИЛИЮ В БЛОКЕ Вопросы и предложения. Пример блока:
Вопросы и предложения Михаил Сазанович ➡️@sazanovich_ma или в общий чат.

ФОРМАТ ОТВЕТА — СТРОГО СЛОВАРЬ:
{{
  "text": "<очищенный текст вакансии>" или null (если отсечена),
  "rate": {{"РФ": <целое число>, "РБ": <целое число>}} или 0 или null,
  "deadline_date": "DD.MM.YYYY" или null,
  "deadline_time": "HH:MM" или null,
  "vacancy_id": "<id или null>",
  "vacancy_title": "<заголовок или null>",
  "acts": true или false,
  "short_project": true или false
  "delay_payment": "<содержимое фразы или null>",
  "only_fulltime": true или false,
  "location": ["РФ", "РБ"] или [],
  "long_payment": true или false
  "reason": "<причина отсева или null>",
  "utochnenie": true или false
}}
Никаких других слов/символов не добавляй. Не используй Markdown/код-блоки в ответе.
"""

    # Модель Gemini
    model = genai.GenerativeModel('gemini-2.5-pro')
    generation_config = genai.types.GenerationConfig(temperature=0.1)
    
    full_prompt = system_prompt + "\n\nТекст вакансии:\n\n" + text

    try:
        response = await model.generate_content_async(
            full_prompt,
            generation_config=generation_config
        )

        clean_text = response.text.strip()
        print("✅ Ответ от Gemini получен.")
        

        # Проверка на отсечение вакансии
        if clean_text.lower() == 'null':
            print("❌ Вакансия отфильтрована.")
            return None

        # Попытка извлечения JSON
        json_match = re.search(r'\{.*\}', clean_text, re.DOTALL)
        if json_match:
            try:
                return json.loads(json_match.group(0))
            except json.JSONDecodeError as e:
                print(f"Ошибка парсинга JSON: {e}")
                return None
        else:
            print("JSON не найден в ответе модели.")
            return None

    except Exception as e:
        print(f"Ошибка при вызове Gemini API: {e}")
        return None



async def format_vacancy_gemini(text: str, vacancy_id: str, date: str | None = None) -> str:
    """
    Функция для структурированного оформления вакансии с помощью Gemini API.
    Возвращает готовый текст в нужном формате.
    """
    if date is None:
        date = datetime.now().strftime("%d.%m.%Y")

    print(f"[*] Форматируем вакансию ID: {vacancy_id} от {date}...")

    prompt = f"""
Ты — редактор вакансий.
На вход ты получаешь текст вакансии. Твоя задача — оформить его в структурированном виде для публикации, строго соблюдая формат.

⚡️ ВАЖНО:
— Никогда не удаляй и не изменяй HTML-тэги (<s>, <b>, <i> и любые другие).
— Особенно не трогай тэги вокруг следующих строк:
  * «ИП: ...»
  * «Самозанятый: ...»
  * «- Ежемесячная выплата Штат/Контракт»
— Эти элементы должны оставаться с тэгами в исходном виде.
— Никогда не добавляй и не убирай теги <s> (или любые другие HTML-теги).
— Если в исходном тексте строка уже содержит <s>…</s> — оставляй как есть.
— Если в исходном тексте строки без <s> — не добавляй зачеркивание самостоятельно.
— Решение о том, какие строки должны быть зачёркнуты, принимается не тобой, а логикой до передачи текста (ты работаешь только с готовым набором тегов).
— Определи, какие страны указаны в тексте:
   * если есть РБ/Беларусь/Belarus/BY → добавь блок 🇧🇾;
   * если есть РФ/Россия/Russia/RU/Москва → добавь блок 🇷🇺;
   * если есть обе — добавь оба блока в указанном порядке (сначала 🇧🇾, потом 🇷🇺);
   * если нет упоминаний — не добавляй флаг, оставь общий блок со ставкой.
— Не придумывай страны, которых нет в тексте.

⚡️ Правила форматирования:
— Всегда выводи строку {date}, даже если в тексте уже есть другая дата.
— Строка {date} должна идти сразу после {vacancy_id}.
— В названии должности всегда указывать грейд (Junior / Middle / Senior / Lead), если он есть.
— Раздел «Будет плюсом» включается в общий список «💻 Требования» (без отдельного блока).
— Каждый пункт требований — отдельной строкой с маркером.
— Между разделами и блоками вакансии должна быть ровно 1 пустая строка.
— В тексте вакансии не должно быть 2 и более пустых строк подряд.
— Не удаляй и не сокращай информацию, используй все данные из текста.
— Блок «⚠️ Особые условия» включай только если в тексте реально есть данные (NDA, кол-во этапов интервью, оформление и т.д.).

🔥 Дополнительное требование:
Если в исходном тексте не указана ставка (RUB, USD, у.е., вилка и т.п.),
в блоке 💰 "Месячная ставка (на руки) до:" вместо суммы напиши:
«Рассматриваем ваши предложения».

⚡️ Структура итогового текста:

🆔{vacancy_id}
📅 Дата публикации: {date}

🥇 [Название должности] ([Грейд])


 🇧🇾💰 Месячная ставка для юр лица РБ:
Вариант 1. Ежемесячная выплата Штат/Контракт (на руки) до: 260 000 RUB (с выплатой зарплаты 11 числа месяца следующего за отчетным)

Вариант 2. Выплата ИП/Самозанятый
С отсрочкой платежа 35 рабочих дней после подписания акта:
(Актирование: ежемесячное):
2700 RUB/час (Gross)
Справочно в месяц (при 170 раб. часов): 445 000 RUB(Gross)


 🇷🇺💰 Месячная ставка для юр лица РФ:
Вариант 1. Ежемесячная выплата Штат/Контракт (на руки) до: 260 000 RUB (с выплатой зарплаты 11 числа месяца следующего за отчетным)

Вариант 2. Выплата ИП/Самозанятый
С отсрочкой платежа 35 рабочих дней после подписания акта:
(Актирование: ежемесячное):
2700 RUB/час (Gross)
Справочно в месяц (при 170 раб. часов): 445 000 RUB(Gross)

Вариант 2. Выплата ИП/Самозанятый: 211 000 RUB


📍 Локация/Гражданство: [страны, города]
🏠 Формат работы: [удалённо / офис / гибрид]
🎓 Грейд: [Junior / Middle / Senior / Lead]
📆 Срок проекта: [сроки]
🚀 Старт проекта: [ASAP или дата]

📌 О проекте:
[Краткое описание проекта]

📎 Задачи:
— [Перечислить задачи из вакансии]

💻 Требования:
— [Обязательное требование 1]
— [Обязательное требование 2]
— [Будет плюсом 1]
— [Будет плюсом 2]

⚠️ Особые условия:
— [NDA, кол-во этапов интервью, трудоустройство и т.д.]

❗️ Обязательные данные по кандидату при подаче:
● ФИО
● Страна + Город
● Дата рождения (не возраст, а дата)
● Электронная почта
● Образование (ВУЗ, год окончания, специальность)
● Грейд
● Ставка
● Чек-лист соответствия требованиям (ДА/НЕТ)

Контакт для вопросов: [имя + @Telegram]

---
Вот текст вакансии для обработки:

{text}
"""

    model = genai.GenerativeModel('gemini-2.5-pro')
    generation_config = genai.types.GenerationConfig(temperature=0.1)

    try:
        response = await model.generate_content_async(prompt, generation_config=generation_config)
        return response.text.strip()
    except Exception as e:
        print(f"🔥 Ошибка при форматировании вакансии: {e}")
        return False

async def format_vacancy_gemini_for_partners(text: str, vacancy_id: str, date: str | None = None) -> str:
    """
    Функция для структурированного оформления вакансии с помощью Gemini API.
    Возвращает готовый текст в нужном формате.
    """
    if date is None:
        date = datetime.now().strftime("%d.%m.%Y")

    print(f"[*] Форматируем вакансию ID: {vacancy_id} от {date}...")

    prompt = f"""
Ты — редактор вакансий.
На вход ты получаешь текст вакансии. Твоя задача — оформить его в структурированном виде для публикации, строго соблюдая формат.

⚡️ ВАЖНО:
— Никогда не удаляй и не изменяй HTML-тэги (<s>, <b>, <i> и любые другие).
— Особенно не трогай тэги вокруг следующих строк:
  * «ИП: ...»
  * «Самозанятый: ...»
  * «- Ежемесячная выплата Штат/Контракт»
— Эти элементы должны оставаться с тэгами в исходном виде.
— Никогда не добавляй и не убирай теги <s> (или любые другие HTML-теги).
— Если в исходном тексте строка уже содержит <s>…</s> — оставляй как есть.
— Если в исходном тексте строки без <s> — не добавляй зачеркивание самостоятельно.
— Решение о том, какие строки должны быть зачёркнуты, принимается не тобой, а логикой до передачи текста (ты работаешь только с готовым набором тегов).
— Определи, какие страны указаны в тексте:
   * если есть РБ/Беларусь/Belarus/BY → добавь блок 🇧🇾;
   * если есть РФ/Россия/Russia/RU/Москва → добавь блок 🇷🇺;
   * если есть обе — добавь оба блока в указанном порядке (сначала 🇧🇾, потом 🇷🇺);
   * если нет упоминаний — не добавляй флаг, оставь общий блок со ставкой.
— Не придумывай страны, которых нет в тексте.

⚡️ Правила форматирования:
— Всегда выводи строку {date}, даже если в тексте уже есть другая дата.
— Строка {date} должна идти сразу после {vacancy_id}.
— В названии должности всегда указывать грейд (Junior / Middle / Senior / Lead), если он есть.
— Раздел «Будет плюсом» включается в общий список «💻 Требования» (без отдельного блока).
— Каждый пункт требований — отдельной строкой с маркером.
— Между разделами и блоками вакансии должна быть ровно 1 пустая строка.
— В тексте вакансии не должно быть 2 и более пустых строк подряд.
— Не удаляй и не сокращай информацию, используй все данные из текста.
— Блок «⚠️ Особые условия» включай только если в тексте реально есть данные (NDA, кол-во этапов интервью, оформление и т.д.).
- Блок "Контакт для вопросов" должен быть всегда "Дмитрий  @DmitriyOmega"

🔥 Дополнительное требование:
Если в исходном тексте не указана ставка (RUB, USD, у.е., вилка и т.п.),
в блоке 💰 "Месячная ставка (на руки) до:" вместо суммы напиши:
«Рассматриваем ваши предложения».

⚡️ Структура итогового текста:

🆔{vacancy_id}
📅 Дата публикации: {date}

🥇 [Название должности] ([Грейд])


 🇧🇾💰 Ставка для подрядичика РБ: 'Ставка из исходного текста' RUB


 🇷🇺💰 Ставка для подрядичика РФ: 'Ставка из исходного текста' RUB


📍 Локация/Гражданство: [страны, города]
🏠 Формат работы: [удалённо / офис / гибрид]
🎓 Грейд: [Junior / Middle / Senior / Lead]
📆 Срок проекта: [сроки]
🚀 Старт проекта: [ASAP или дата]

📌 О проекте:
[Краткое описание проекта]

📎 Задачи:
— [Перечислить задачи из вакансии]

💻 Требования:
— [Обязательное требование 1]
— [Обязательное требование 2]
— [Будет плюсом 1]
— [Будет плюсом 2]

⚠️ Особые условия:
— [NDA, кол-во этапов интервью, трудоустройство и т.д.]

❗️ Обязательные данные по кандидату при подаче:
● ФИО
● Страна + Город
● Дата рождения (не возраст, а дата)
● Электронная почта
● Образование (ВУЗ, год окончания, специальность)
● Грейд
● Ставка
● Чек-лист соответствия требованиям (ДА/НЕТ)

Контакт для вопросов: Дмитрий  @DmitriyOmega

---
Вот текст вакансии для обработки:

{text}
"""

    model = genai.GenerativeModel('gemini-2.5-pro')
    generation_config = genai.types.GenerationConfig(temperature=0.1)

    try:
        response = await model.generate_content_async(prompt, generation_config=generation_config)
        return response.text.strip()
    except Exception as e:
        print(f"🔥 Ошибка при форматировании вакансии: {e}")
        return False


async def generate_hashtags_gemini(vacancy_text: str) -> str:
    """
    Генерирует хэштеги из текста вакансии с помощью Gemini API.
    """
    print("[*] Генерируем хэштеги...")
    
    prompt = f"""
Вот текст вакансии:

{vacancy_text}

Сформируй список хэштегов в одну строку через пробел.

Правила:
1. По технологиям/стеку (например: #Java #Spring #PostgreSQL #Docker) — извлеки их из описания.
2. По грейду: #Junior или #Middle или #Senior — определи из текста.
3. По отрасли (например: #Fintech #Retail #Banking) — определи по контексту.

Выведи только строку с хэштегами.
"""
    
    model = genai.GenerativeModel('gemini-2.5-flash')
    generation_config = genai.types.GenerationConfig(temperature=0.1)
    
    try:
        response = await model.generate_content_async(prompt, generation_config=generation_config)
        return response.text.strip()
    except Exception as e:
        print(f"🔥 Ошибка при генерации хэштегов: {e}")
        return False





async def sverka_vac_and_resume_json(vacancy_text: str, resume_text: str, file_name: str):
    """
    Анализирует соответствие кандидата вакансии и возвращает JSON от Gemini без пост-обработки.

    Логика Omega Solutions v3.6:

    — Всегда возвращаем ОДИН валидный JSON-объект (response_mime_type='application/json').
    — Не используем null: вместо этого — строковые статусы "Нет (требуется уточнение)" / "Нет (точно нет)" или "нужно уточнить" для salary.
    — Все требования из вакансии должны быть отражены 1-в-1 в vacancy.requirements.* и в compliance_check.*.
    — Для must_have есть поле importance: critical / important / supporting.
    — "Полностью подходит" = кандидат соответствует ВСЕМ требованиям must_have на 100%, локация ок, ставка не перегрета >30%.
    — "Не подходит" = локация заведомо не подходит, или есть профессиональные блокеры (Нет (точно нет)), или ставка > +30% к бюджету.
    """

    prompt = f"""
Ты — продвинутый ассистент IT-рекрутера компании Omega Solutions.
Твоя задача — оценить соответствие кандидата требованиям вакансии и вернуть строго ВАЛИДНЫЙ JSON-объект без текста до или после.

Никакого Markdown, комментариев или пояснений вне JSON — только один JSON.

================================
1. ВХОДНЫЕ ДАННЫЕ
================================

Имя файла кандидата (строка для разбора): "{file_name}"

[ВАКАНСИЯ]
{vacancy_text}

[РЕЗЮМЕ]
{resume_text}

================================
2. ЦЕЛИ АНАЛИЗА
================================

— Провести профессиональную сверку резюме и вакансии:
  — извлечь все требования из вакансии и разнести их на must_have и nice_to_have;
  — по каждому требованию выставить статус соответствия кандидата;
  — присвоить каждому must_have уровень важности (importance: critical / important / supporting);
  — проверить полноту резюме по ключевым полям: ФИО, дата рождения, локация, контакты, образование, зарплатные ожидания;
  — оценить локацию и гражданство кандидата относительно требований вакансии;
  — оценить зарплатные ожидания кандидата относительно бюджета вакансии;
  — определить итоговый вердикт по кандидату (summary.verdict).

— Вернуть результат строго в формате JSON по схеме, описанной ниже.

================================
3. ОБЩИЕ ПРАВИЛА ФОРМАТА
================================

1. Всегда возвращай только один валидный JSON-объект.
2. Не используй null. Если данных нет:
   — для полей и статусов — ставь "Нет (требуется уточнение)" или "Нет (точно нет)";
   — для salary_expectations в candidate и summary — используй строку "нужно уточнить".
3. Все имена, города, страны приводи к русскому языку (транслитерируй/переводи по смыслу).
4. Если информации нет вообще или есть явное противоречие — используй "Нет (точно нет)" (для профессиональных требований).
5. Формат дат — ДД.ММ.ГГГГ, возраст округляй до целых лет.

================================
4. АНАЛИЗ ИМЕНИ ФАЙЛА И ЗАРПЛАТЫ
================================

Разбери строку имени файла "{file_name}" и используй её как дополнительный источник данных:

1) Раздели строку на токены по символам "_", "-", ".", пробелам.
2) Если токен содержит кириллицу или латиницу и начинается с заглавной буквы — это потенциальная часть ФИО.
   — Первые два таких токена интерпретируй как:
     • первое — фамилия,
     • второе — имя.
   Примеры:
   • Ivan_Ivanov_BE10451.docx → Иван Иванов
   • Kurs_Aleksandr_Resume.docx → Александр Курс
   • Александр Курс 2200 у.е..docx → Александр Курс

3) Если токен содержит число + валюту (RUB, руб, ₽, USD, $, у.е, EUR и т.п.) — это потенциальные зарплатные ожидания.
   Примеры:
   • Ivan_Ivanov_3500USD.docx → "3500 USD"
   • Александр Курс 2200 у.е..docx → "2200 у.е."

4) Если в числе есть пробелы — убери их: "400 000 RUB" → "400000 RUB".
5) Если валюта указана буквами (RUB, USD, у.е.) — сохраняй как есть, не конвертируй.
6) Если зарплата найдена в резюме или имени файла — запиши её:
   — candidate.salary_expectations = "<Сумма с валютой>";
   — summary.salary_expectations = "<Сумма с валютой>".

   Если указано "от N" или диапазон "от N до M":
   — минимальной зарплатной ожиданием считаем "N" (именно значение "от").

7) Если ни в резюме, ни в названии файла нет зарплатных ожиданий:
   — candidate.salary_expectations = "нужно уточнить";
   — summary.salary_expectations = "нужно уточнить";
   — ДОБАВЬ в compliance_check.must_have пункт:
     {{
       "requirement": "Не указаны зарплатные ожидания",
       "status": "Нет (требуется уточнение)",
       "comment": "Зарплатные ожидания не определены ни в резюме, ни в названии файла",
       "importance": "important"
     }}

================================
5. ТРЕБОВАНИЯ К ИМЕНИ (ФИ или ФИО)
================================

По тексту вакансии {vacancy_text} определи, какой формат имени требуется:

— Если явно встречается "ФИО" или формулировки вида "полное ФИО" → требуется Фамилия Имя Отчество.
— Если встречаются "ФИ", "Фамилия Имя" или аналогичные формулировки без слова "Отчество" → достаточно Фамилии и Имени, отчество НЕ обязательно.

Работа с candidate.full_name:

— candidate.full_name должен быть строкой:
  — "Фамилия Имя Отчество" (если отчество указано),
  — или "Фамилия Имя" (если отчество отсутствует или не требуется).

Правила:

1) Если нельзя надёжно определить фамилию и имя ни по резюме, ни по имени файла → добавь в compliance_check.must_have:

   {{
     "requirement": "Наличие в резюме: ФИО",
     "status": "Нет (требуется уточнение)",
     "comment": "Не удаётся надёжно определить фамилию и имя кандидата по резюме и имени файла",
     "importance": "critical"
   }}

2) Если по тексту вакансии требуется именно ФИО, а в full_name указаны только фамилия и имя (без отчества) → добавь:

   {{
     "requirement": "Наличие в резюме: ФИО",
     "status": "Нет (требуется уточнение)",
     "comment": "В вакансии требуется полное ФИО, в резюме отсутствует отчество",
     "importance": "important"
   }}

3) Если формат "ФИ" (отчество не требуется) и фамилия+имя определены — отдельный пункт "Наличие в резюме: ФИО" НЕ создавай.

Для проверки полноты ФИО используется только стандартизированный лейбл:
— "Наличие в резюме: ФИО".
Отдельные лейблы "Имя", "Фамилия", "Отчество" НЕ использовать.

================================
6. ЛОКАЦИЯ И ГРАЖДАНСТВО
================================

По тексту вакансии определи требования к локации/гражданству:
— допустимые страны/города;
— явно запрещённые страны (например, Польша);
— требования "только РФ", "только РБ", "только ЕАЭС" и т.п.

Сопоставь с candidate.location и, при наличии, с гражданством (если оно указано в резюме).

Если:

— Локация или гражданство кандидата ЯВНО не входят в допустимый список вакансии,
  ИЛИ
— локация/гражданство кандидата попадает в явно запрещённый список
  (например, в вакансии указано, что кандидаты из Польши не рассматриваются, а кандидат живёт в Польше),

ТО:

1) Добавь в compliance_check.must_have требование:

   {{
     "requirement": "Соответствие локации требованиям вакансии",
     "status": "Нет (точно нет)",
     "comment": "Локация/гражданство кандидата противоречит требованиям вакансии",
     "importance": "critical"
   }}

2) Это требование должно учитываться как профессиональный блокер для вердикта "Не подходит".

Если локация и гражданство соответствуют или их можно считать совместимыми с вакансией,
можно добавить внутреннюю проверку, но в финальный JSON "Наличие в резюме: Локация" выводить ТОЛЬКО если данных не хватает (см. блок про «Наличие в резюме: …»).

================================
7. ЛОГИКА СОВПАДЕНИЙ ПО ТРЕБОВАНИЯМ
================================

Фиксированный словарь статусов для compliance_check.*:
— "Да"
— "Нет (требуется уточнение)"
— "Нет (точно нет)"  (только для профессиональных требований и специальных блокеров типа локации/бюджета)

Каждому элементу compliance_check.must_have нужно добавить поле importance:
— "critical" — ключевой навык/условие (основной стек, стаж, обязательная локация/гражданство, жёсткий формат занятости и т.п.).
— "important" — важные, но теоретически обучаемые или компенсируемые навыки.
— "supporting" — вспомогательные инструменты (мониторинг, отдельные тулзы, дополнительные фреймворки и т.п.).

Примеры классификации:

— critical:
  • "Опыт коммерческой разработки на Java от 5 лет"
  • "Опыт коммерческой разработки на React от 3 лет"
  • "Гражданство РФ" (если явно требуется)
  • "Работа в офисе в Москве, полная занятость"

— important:
  • "Опыт с Spring Boot"
  • "Опыт разработки микросервисов"
  • "Опыт работы в высоконагруженных системах"

— supporting:
  • "Опыт работы с Kibana / Grafana"
  • "Опыт работы с Jira"
  • "Опыт работы с GitLab CI/CD"

Правила статусов:

— Если обязательное требование явно и полностью отсутствует, и из резюме видно, что опыта точно нет или он меньше требуемого → "Нет (точно нет)".
— Если навык/технология указаны частично (Java вместо Java 21, Spring вместо Spring Boot, общий DevOps вместо конкретного инструмента) → "Нет (требуется уточнение)".
— Если указана другая версия, но есть релевантный опыт (Java 8 вместо Java 21, опыт 3 года вместо 5 при требовании 5+ лет) → "Нет (требуется уточнение)".
— Если в резюме нет упоминаний по вспомогательным инструментам (Jira, Docker, CI/CD и т.п.), но они могли фактически быть → "Нет (требуется уточнение)".
— Если явно указано, что стаж/уровень МЕНЬШЕ требуемого и не может быть компенсирован → "Нет (точно нет)".
— Если информации недостаточно или есть сомнения — всегда "Нет (требуется уточнение)".

Интерпретация:

— "Да" = требование явно подтверждено.
— "Нет (требуется уточнение)" = информация неполная, косвенная, другая версия, нет детализации.
— "Нет (точно нет)" = очевидное несоответствие, которое нельзя компенсировать (даже после уточнений не выйдем на 100% соответствие).

================================
8. ВЕРДИКТ (summary.verdict)
================================

Вердикт зависит ТОЛЬКО от must_have и специальных блокеров (локация, бюджет и т.п.).

1) summary.verdict = "Полностью подходит", ЕСЛИ И ТОЛЬКО ЕСЛИ:

   — Для КАЖДОГО требования в compliance_check.must_have:
     • status = "Да";
   — Нет ни одного "Нет (требуется уточнение)" и ни одного "Нет (точно нет)" в must_have;
   — Локация и гражданство кандидата совместимы с требованиями вакансии
     (нет требования "Соответствие локации требованиям вакансии" со статусом "Нет (точно нет)");
   — Зарплатные ожидания кандидата НЕ превышают верхний порог ставки вакансии для соответствующей юрисдикции (РБ или РФ) более чем на 30%.
     (См. блок про зарплаты ниже.)

2) summary.verdict = "Не подходит", ЕСЛИ:

   — Есть хотя бы одно профессиональное требование в compliance_check.must_have со статусом "Нет (точно нет)" и importance = "critical"
     (ключевые навыки, стаж, строгие требования по локации/гражданству/формату/бюджету),
     ИЛИ
   — Локация/гражданство кандидата ЯВНО не входят в допустимые по вакансии или входят в явно запрещённый список,
     ИЛИ
   — Минимальные зарплатные ожидания кандидата (его "от") превышают верхний порог вилки вакансии
     для соответствующей юрисдикции (РБ — для кандидатов с локацией в Беларуси, РФ — для кандидатов с локацией в России)
     более чем на 30%, и по тексту вакансии этот верхний порог можно надёжно извлечь.

   Пункты "Наличие в резюме: ФИО", "Наличие в резюме: Локация", "Наличие в резюме: Дата рождения",
   "Наличие в резюме: Контакты", "Наличие в резюме: Образование", а также "Не указаны зарплатные ожидания"
   сами по себе НЕ должны приводить к вердикту "Не подходит".
   Они только переводят кандидата в "Частично подходит (нужны уточнения)".

3) summary.verdict = "Частично подходит (нужны уточнения)" во всех остальных случаях, когда:

   — Нет профессиональных must_have со статусом "Нет (точно нет)" и importance = "critical";
   — Но есть хотя бы один must_have со статусом "Нет (требуется уточнение)" или "Нет (точно нет)" c importance = "important" или "supporting".

================================
9. ЗАРПЛАТНЫЕ ОЖИДАНИЯ И БЮДЖЕТ ВАКАНСИИ
================================

Из текста вакансии постарайся извлечь вилку ставки (минимум и максимум), особенно если:

— указаны разные ставки для:
  • юрлица в Республике Беларусь,
  • юрлица в РФ.

Примеры форматов:

— "ставка: до 2300 РФ / 2600 РБ"
— "вилка: 1500–2000 USD"
— "зарплата: от 1800 до 2200 EUR"

Правила:

1) Если по тексту вакансии можно надёжно определить верхний порог ставки для юрисдикции, соответствующей локации кандидата:

   — Для кандидата с локацией в Беларуси используй верхний порог для юрлица РБ.
   — Для кандидата с локацией в России используй верхний порог для юрлица РФ.
   — Для других стран используй общий верхний порог, если он указан.

2) Сравни минимальные зарплатные ожидания кандидата (значение "от") с этим верхним порогом:

   — Если ожидания кандидата превышают верхний порог более чем на 30%:
     • добавь в compliance_check.must_have требование:

       {{
         "requirement": "Зарплатные ожидания в рамках бюджета вакансии",
         "status": "Нет (точно нет)",
         "comment": "Ожидания кандидата превышают верхний порог бюджета вакансии более чем на 30%",
         "importance": "critical"
       }}

     • это требование должно приводить к вердикту "Не подходит".

   — Если ожидания кандидата находятся в пределах +30% от верхнего порога или ниже:
     • можешь добавить (опционально) требование с статусом "Да" или "Нет (требуется уточнение)",
       но без "Нет (точно нет)", если нет жёсткого конфликта.

3) Если по тексту вакансии нельзя надёжно определить верхний порог ставки:
   — НЕ ставь "Нет (точно нет)" по бюджету;
   — в таком случае вердикт по бюджету не должен быть "Не подходит только по цифрам".

================================
10. СТРУКТУРА JSON
================================

JSON-объект ДОЛЖЕН строго соответствовать этой структуре (без null):

{{
  "vacancy": {{
    "position_id": "<ID вакансии или Нет (требуется уточнение)>",
    "position_name": "<Название позиции или Нет (требуется уточнение)>",
    "grade": "<Грейд или Нет (требуется уточнение)>",
    "location_requirements": {{
      "location": "<Локация или Нет (требуется уточнение)>",
      "citizenship": "<Гражданство или Нет (требуется уточнение)>",
      "timezone": "<Часовой пояс или Нет (требуется уточнение)>"
    }},
    "work_format": "<remote/office/hybrid или Нет (требуется уточнение)>",
    "tech_stack": ["<Технология 1>", "<Технология 2>"],
    "requirements": {{
      "must_have": ["<Обязательное требование 1>", "<Обязательное требование 2>"],
      "nice_to_have": ["<Дополнительное требование 1>", "<Дополнительное требование 2>"]
    }},
    "special_conditions": {{
      "nda": "<да/нет/Нет (требуется уточнение)>",
      "sim_card": "<требуется/не требуется/Нет (требуется уточнение)>",
      "rate": "<ставка или Нет (требуется уточнение)>",
      "deadlines": "<дедлайны или Нет (требуется уточнение)>"
    }},
    "manager_telegram_nickname": "@Dimitryver"
  }},
  "candidate": {{
    "full_name": "<Фамилия Имя Отчество или Фамилия Имя>",
    "birth_date": {{
      "date": "<ДД.ММ.ГГГГ или Нет (требуется уточнение)>",
      "age": "<число лет или Нет (требуется уточнение)>"
    }},
    "location": {{
      "city": "<Город или Нет (требуется уточнение)>",
      "country": "<Страна или Нет (требуется уточнение)>"
    }},
    "grade_and_position": "<Грейд/позиция кандидата или Нет (требуется уточнение)>",
    "full_experience": "<Опыт в IT или Нет (требуется уточнение)>",
    "experience": [
      {{
        "company_name": "<Компания>",
        "role": "<Роль>",
        "domain": "<Домен или Нет (требуется уточнение)>",
        "period": "<Период работы>",
        "projects": [
          {{
            "project_description": "<Описание проекта>",
            "responsibilities": ["<Обязанность 1>", "<Обязанность 2>"],
            "technologies_used": ["<Технология 1>", "<Технология 2>"]
          }}
        ]
      }}
    ],
    "tech_stack": ["<Технология 1>", "<Технология 2>"],
    "languages": [
      {{
        "language": "<Язык>",
        "level": "<Уровень>"
      }}
    ],
    "contacts": {{
      "phone": "<Телефон или Нет (требуется уточнение)>",
      "email": "<Email или Нет (требуется уточнение)>",
      "telegram": "<Telegram или Нет (требуется уточнение)>",
      "linkedin": "<LinkedIn или Нет (требуется уточнение)>"
    }},
    "salary_expectations": "<Сумма или нужно уточнить>"
  }},
  "compliance_check": {{
    "must_have": [
      {{
        "requirement": "<требование>",
        "status": "<Да | Нет (требуется уточнение) | Нет (точно нет)>",
        "comment": "<обоснование статуса>",
        "importance": "<critical | important | supporting>"
      }}
    ],
    "nice_to_have": [
      {{
        "requirement": "<требование>",
        "status": "<Да | Нет (требуется уточнение)>",
        "comment": "<обоснование статуса>"
      }}
    ]
  }},
  "summary": {{
    "verdict": "<Полностью подходит | Частично подходит (нужны уточнения) | Не подходит>",
    "salary_expectations": "<Сумма или 'нужно уточнить'>"
  }}
}}

================================
11. ОПРЕДЕЛЕНИЕ ID ВАКАНСИИ
================================

Айди вакансии (vacancy.position_id) — это цифры и/или буквы в начале вакансии, например:
— "10261"
— "QA-10261"
— "1C-10261"

Айди должен быть извлечён из текста вакансии и записан в vacancy.position_id.
Если однозначно определить нельзя → "Нет (требуется уточнение)".

================================
12. ИЗВЛЕЧЕНИЕ ТРЕБОВАНИЙ И СИНХРОНИЗАЦИЯ
================================

РЕЖИМ «ОДНА СТРОКА → ОДИН ПУНКТ» (СТРОГО ОБЯЗАТЕЛЬНО)

— Каждое требование берётся целиком одной строкой ровно в том виде, как в тексте вакансии
  (включая запятые, скобки, "/", "или", двоеточия, примеры и т.п.).
— Запрещено разбивать одну строку из вакансии на несколько требований.
— Порядок строк сохраняется.

Формирование массивов:

— vacancy.requirements.must_have: массив строк обязательных требований, 1 строка = 1 элемент.
— vacancy.requirements.nice_to_have: массив строк дополнительных требований, 1 строка = 1 элемент.

Синхронизация:

— Для каждого элемента vacancy.requirements.must_have[*] должен быть РОВНО ОДИН элемент
  compliance_check.must_have[*] с тем же текстом в поле "requirement".
— Для каждого элемента vacancy.requirements.nice_to_have[*] должен быть РОВНО ОДИН элемент
  compliance_check.nice_to_have[*] с тем же текстом в "requirement".

Инварианты:

— len(vacancy.requirements.must_have) == len(compliance_check.must_have)
— len(vacancy.requirements.nice_to_have) == len(compliance_check.nice_to_have)
— Нельзя добавлять в compliance_check требования, которых нет в vacancy.requirements.
— Нельзя терять требования из вакансии.

Интерпретация AND/OR внутри строки (Только для статуса и комментария, текст НЕ меняется):

— Если перечисление через запятые без "или" / "/" → трактуй как AND:
  • статус "Да" только если подтверждены ВСЕ элементы;
  • иначе "Нет (требуется уточнение)" или "Нет (точно нет)", если явно чего-то нет.

— Если в строке есть "или" или "/" между элементами → трактуй как OR:
  • достаточно подтверждения ЛЮБОГО элемента для статуса "Да";
  • если все альтернативы не подтверждены → "Нет (требуется уточнение)" или "Нет (точно нет)", если явно нет.

Комментарий (comment):

— Для AND: кратко укажи, что подтверждено, чего не хватает.
— Для OR: какие альтернативы подтверждены / не подтверждены.

================================
13. СТАНДАРТИЗАЦИЯ ЛЕЙБЛОВ «НАЛИЧИЕ В РЕЗЮМЕ: …»
================================

Всегда используй ровно такие лейблы:

— "Наличие в резюме: ФИО"
— "Наличие в резюме: Локация"
— "Наличие в резюме: Дата рождения"
— "Наличие в резюме: Контакты"
— "Наличие в резюме: Образование"

Особые правила:

— Для "Наличие в резюме: …" НИКОГДА не используй статус "Нет (точно нет)".
— Если статус таких пунктов = "Да" → перед финальным выводом УДАЛИ эти элементы из compliance_check.must_have / nice_to_have (они не должны попадать в итоговый JSON).
— В финальном JSON допускаются только те "Наличие в резюме: …", у которых статус = "Нет (требуется уточнение)".

================================
14. КОНТАКТЫ
================================

Определяй наличие контактов по candidate.contacts:
— phone
— email
— telegram
— linkedin

Правило:

— Если ХОТЯ БЫ ОДНО из этих полей присутствует и не равно "Нет (требуется уточнение)" →
  пункт "Наличие в резюме: Контакты" НЕ создавай.

— Если ВСЕ четыре поля отсутствуют/пустые/равны "Нет (требуется уточнение)" →
  создаёшь РОВНО ОДИН пункт:

{{
  "requirement": "Наличие в резюме: Контакты",
  "status": "Нет (требуется уточнение)",
  "comment": "Отсутствуют телефон, e-mail, Telegram и LinkedIn",
  "importance": "critical"
}}

И при этом не создавай отдельных пунктов:
— "Наличие в резюме: Телефон"
— "Наличие в резюме: Email"
— "Наличие в резюме: Telegram"
— "Наличие в резюме: LinkedIn"
Такие отдельные лейблы запрещены.

================================
15. ДЕТЕРМИНИЗМ И НОРМАЛИЗАЦИЯ
================================

— Используй строго фиксированный словарь статусов:
  • "Да"
  • "Нет (требуется уточнение)"
  • "Нет (точно нет)" (только для профессиональных требований и блокеров).

— Нормализуй стек:
  • приводи к нижнему регистру;
  • убирай диакритику, дефисы/точки/подчеркивания внутри токенов;
  • применяй словарь синонимов:

    — js → javascript; ts → typescript; nodejs → node.js; reactjs → react; react.js → react; vue.js → vue; angularjs → angular
    — k8s → kubernetes; gke → google kubernetes engine; eks → aws elastic kubernetes service; aks → azure kubernetes service
    — aws → amazon web services; gcp → google cloud platform; azure → microsoft azure
    — postgres → postgresql; mssql → sql server; es → elasticsearch; elk → elasticsearch logstash kibana
    — ci/cd → cicd; gitlab-ci → cicd; github actions → cicd; jenkins → cicd
    — spring → spring framework; springboot → spring boot; jpa → hibernate jpa
    — py → python; tf → terraform; docker-compose → docker compose

— Формат занятости:
  • remote → remote
  • офис/office → office
  • гибрид/hybrid → hybrid

— Для зарплат:
  • распознавай 12 000, 12,000, 12000, 12k, 12K → 12k трактуй как 12000;
  • валюты: ₽, руб, RUB; $, USD; €, EUR; "у.е." трактуй как условные единицы (аналог USD), но не конвертируй.

— Для дат:
  • распознавай ДД.ММ.ГГГГ, ГГГГ-ММ, ММ/ГГГГ; в выводе всегда ДД.ММ.ГГГГ.

При противоречиях:

— Если в резюме и вакансии есть противоречие:
  1) приоритет явных формулировок в описаниях опыта;
  2) затем — сводный список навыков;
  3) затем — просто ключевые слова.
— Если доказательства равноценны → "Нет (требуется уточнение)".
— Запрещены домыслы без опоры на текст. Любая неочевидная интерпретация = "Нет (требуется уточнение)".

================================
16. КОНТРОЛЬ ПЕРЕД ВЫВОДОМ JSON
================================

Перед выдачей JSON убедись, что:

— Все требования из вакансии, которые можно интерпретировать как требования, присутствуют в vacancy.requirements.must_have или nice_to_have.
— Все строки из vacancy.requirements.* 1-в-1 отражены в compliance_check.* по полю "requirement".
— Нет дубликатов и пустых строк в требованиях.
— Выполнены инварианты:
  • len(vacancy.requirements.must_have) == len(compliance_check.must_have)
  • len(vacancy.requirements.nice_to_have) == len(compliance_check.nice_to_have)
— Для "Наличие в резюме: …" нет элементов со статусом "Да" (они удалены).
— Для этих лейблов не используется статус "Нет (точно нет)".
— Для отсутствующих контактов создан ровно один пункт "Наличие в резюме: Контакты" (если нужно).
— Если все пять проверок полноты (ФИО, Локация, Дата рождения, Контакты, Образование) пройдены, в compliance_check.* не остаётся ни одного требования "Наличие в резюме: …".

После выполнения всех проверок — верни ТОЛЬКО JSON-объект, без текста до или после.
"""

    model = genai.GenerativeModel("gemini-2.5-pro")
    generation_config = genai.GenerationConfig(
        temperature=0.1,
        response_mime_type="application/json",
    )

    response = await model.generate_content_async(
        prompt,
        generation_config=generation_config,
    )

    raw_json = response.text.strip()
    return raw_json
    







async def generate_mail_for_candidate_finalist(json_data: dict, user_name: str, group_id: int, thread_id: int):
  try:
    group_id = str(group_id)
    group_id = group_id.replace("-100", "")
    thread_id = str(thread_id)
    vacancy = json_data.get('vacancy', {})
    candidate = json_data.get('candidate', {})
    vac_id = vacancy.get('position_id', 'ID вакансии')
    
    
    must_have_candidate = json_data.get('compliance_check', {}).get('must_have', [])
    strong_points = []
    for must_have in must_have_candidate:
      if must_have['status'] == 'Да':
        strong_points.append(must_have['requirement'])
      elif must_have['status'] == 'Нет (требуется уточнение)':
        continue
      elif must_have['status'] == 'Нет (точно нет)':
        continue
    
    prompt = f"""
    Ты — ассистент IT-рекрутера. 
  Ты должен составить персонализированное письмо кандидату-финалисту.

  ⚙️ Формат вывода: только готовый текст письма, без Markdown, без пояснений и меток кода.

  ---

  🎯 Цель письма:
  1. Поблагодарить кандидата за отклик.
  2. Коротко и ясно представить вакансию (позиция, стек, формат, условия).
  3. Упомянуть сильные стороны кандидата (2–4 пункта, по данным из анализа).
  4. Пригласить подтвердить готовность к интервью.
  5. Использовать деловой, вежливый, но дружелюбный стиль (без излишнего официоза).

  ---

  📩 Структура письма:

  1️⃣ Приветствие:
  «Здравствуйте, {candidate['full_name']}!»  
  — Используй полное имя, включая отчество, если оно указано в JSON.  
  — Если отчество отсутствует, обращайся по имени и фамилии.

  2️⃣ Благодарность:
  «Компания Omega Solutions благодарит вас за предоставленное резюме и интерес к нашим вакансиям.»

  3️⃣ Представление вакансии:

  - Укажи позицию {vacancy['position_name']}
  - Укажи айди вакансии <a href="https://t.me/c/{group_id}/{thread_id}">{vac_id}</a>
  - Укажи кратко 1–2 ключевые задачи (возьми из списка {vacancy['requirements']['must_have']})
  - Перечисли кратко стек технологий ({vacancy['tech_stack']}).
  - Опиши формат работы ({vacancy['work_format']}: офис / удалённо / гибрид).
  - Укажи особые условия, если есть ({vacancy['special_conditions']}, например NDA, SIM-карта, ставка и т. д.
  - Удали любые упоминания об условиях оплаты и зарплаты.
  - Не удаляй ссылку в айди вакансии.

  4️⃣ Сильные стороны кандидата:
  - Используй 2–4 релевантных пункта на основе списка {strong_points}.
  - Примеры: «Опыт работы с Python более 3 лет», «Разработка микросервисов», «Знание Docker и Kubernetes», «Опыт удалённой работы в распределённой команде».

  5️⃣ Заключение:
  «Если вам интересна данная позиция, просим Вас дать согласие на предоставление Вашего резюме на рассмотрение нашим Заказчикам. Если они остановятся на выборе Вашей кандидатуры, мы обязательно свяжемся с Вами,  обсудим детали и удобное для Вас время для прохождения интервью.»

  6️⃣ Подпись:
  «С уважением,  
  {user_name}/Omega Solutions»
  ---

  ✍️ Твоя задача:
  Составь готовое письмо кандидату по вышеуказанным правилам на русском языке.
  Не используй шаблоны вроде “Уважаемый кандидат”, всегда подставляй имя.
  Не добавляй пояснений, просто выведи готовый текст письма.
  Удали в итоговом тексте любые упоминания об условиях оплаты , ставки, зарплаты и т.д.
  Сделай текст читаемым и визуально красивым. Вот пример:
  
  Здравствуйте, Максим Александрович!

  Компания Omega Solutions благодарит вас за предоставленное резюме и интерес к нашим вакансиям.Мы рассматриваем вас на позицию Разработчик-эксперт BPMSoft (<a href="https://t.me/c/{group_id}/{thread_id}">{vac_id}</a>) в рамках долгосрочного проекта по замещению CRM B2C.

  📌 Кратко о вакансии:
  — Разработка программных модулей BPMSoft по техническим требованиям;
  — Участие в обсуждении архитектуры и требований;
  — Интеграции с внешними системами;
  — Формат — удалённая полная занятость, поквартальная оплата.

  Ваш опыт по BPMSoft, C#, Ext JS и Angular полностью соответствует требованиям вакансии.
  Чтобы завершить сверку, пожалуйста, уточните:
  — используете ли вы в работе Kafka или аналогичный брокер сообщений?
  — работали ли вы с Swagger (OAS) и Postman для тестирования REST API?
  — применяете ли вы GitLab в практической разработке?

  Если вам интересна данная позиция, просим Вас дать согласие на предоставление Вашего резюме на рассмотрение нашим Заказчикам. Если они остановятся на выборе Вашей кандидатуры, мы обязательно свяжемся с Вами,  обсудим детали и удобное для Вас время для прохождения интервью.
  С уважением,
  {user_name}/Omega Solutions
  """

    model = genai.GenerativeModel('gemini-2.5-pro')
    generation_config = genai.types.GenerationConfig(temperature=0.1)

    
    response = await model.generate_content_async(prompt, generation_config=generation_config)
    return response.text.strip()
  except Exception as e:
      print(f"🔥 Ошибка при формировании письма кандидату: {e}")
      return False
    
    
    
def _canon_token(s: str) -> str:
    """
    Приводим строку к каноническому виду для сравнения технологий:
    — нижний регистр;
    — убираем пробелы.
    """
    return "".join(ch for ch in s.lower() if not ch.isspace())


def _extract_candidate_stack(candidate: dict) -> list[str]:
    """
    Достаём стек кандидата из candidate['tech_stack'].

    Поддерживаем варианты:
    — строка "JavaScript, TypeScript, React"
    — список ["JavaScript", "TypeScript, React"]
    — иначе возвращаем пустой список.
    """
    raw = candidate.get("tech_stack", [])
    items: list[str] = []

    if isinstance(raw, str):
        parts = raw.replace(";", ",").split(",")
        items = [p.strip() for p in parts if p.strip()]
    elif isinstance(raw, list):
        for el in raw:
            if isinstance(el, str):
                parts = el.replace(";", ",").split(",")
                items.extend(p.strip() for p in parts if p.strip())

    # убираем дубли, сохраняем порядок
    seen = set()
    result = []
    for it in items:
        key = _canon_token(it)
        if key not in seen:
            seen.add(key)
            result.append(it)
    return result


def build_questions_block(must_have_candidate: list[dict], candidate: dict) -> str:
    """
    Строит готовый блок уточняющих вопросов для кандидата на основе:
    — must_have_candidate: список dict с полями "requirement", "status"
    — candidate: dict, берём из него tech_stack

    Логика (вариант B):
    — Включаем ВСЕ требования, у которых статус не "Да" и не "не относится".
    — Для анкетных пунктов ("Наличие в резюме", "Не указаны зарплатные ожидания") — отдельные шаблоны.
    — Для стеков с перечислением технологий — спрашиваем только те технологии, которых нет в стеке кандидата.
    — Для общих профессиональных требований — задаём вопрос по требованию целиком, с учётом статуса.
    """
    candidate_stack_list = _extract_candidate_stack(candidate)
    candidate_stack_canon = {_canon_token(t) for t in candidate_stack_list}

    NEGATIVE_MARKERS = (
        "нет",
        "частично",
        "требует уточнения",
        "требуется уточнение",
        "не указано",
    )

    issues: list[tuple[str, str]] = []

    for must in must_have_candidate:
        requirement = (must.get("requirement") or "").strip()
        status = (must.get("status") or "").strip()
        if not requirement:
            continue

        status_norm = status.lower()

        # Явное "Да" — пропускаем
        if status_norm == "да":
            continue

        # Спец-случай "не относится" (если такой статус у вас используется)
        if "не относится" in status_norm:
            continue

        # Все статусы с маркерами "нет/частично/требует/не указано" — берём в issues
        if any(marker in status_norm for marker in NEGATIVE_MARKERS):
            issues.append((requirement, status))

    # Убираем дубли требований, сохраняем порядок
    seen_req = set()
    uniq_issues: list[tuple[str, str]] = []
    for req, st in issues:
        if req not in seen_req:
            seen_req.add(req)
            uniq_issues.append((req, st))

    if not uniq_issues:
        return ""  # нет вопросов — блок не нужен

    questions: list[str] = []

    for requirement, status in uniq_issues:
        req_lower = requirement.lower()

        # 1) Анкетные поля / общие данные
        if requirement.startswith("Наличие в резюме:"):
            field = requirement.split(":", 1)[1].strip().lower()

            if "фио" in field:
                q = "просим указать ваше полное ФИО."
            elif "дата рождения" in field:
                q = "просим указать вашу дату рождения."
            elif "локация" in field or "город" in field:
                q = "просим указать вашу текущую локацию (страна и город)."
            else:
                q = f"просим указать: {requirement.split(':', 1)[1].strip()}."
            questions.append(q)
            continue

        if "зарплат" in req_lower:
            q = "просим указать ваши зарплатные ожидания."
            questions.append(q)
            continue

        # 2) Требования по стеку/технологиям:
        #    Пробуем вытащить конкретные технологии из части после двоеточия.
        #    Например:
        #    "Знание стека: Java Script, Type Script, C#"
        #    "Опыт работы с Typescript, Webpack, Git, Docker, Kubernetes, Nginx"
        after_colon = requirement.split(":", 1)[1].strip() if ":" in requirement else requirement
        tech_parts = [p.strip() for p in after_colon.split(",") if p.strip()]

        req_techs: list[tuple[str, str]] = [(p, _canon_token(p)) for p in tech_parts]

        # Отбираем технологии, которых нет в стеке кандидата
        missing_techs: list[str] = []
        for orig, canon in req_techs:
            if canon and canon not in candidate_stack_canon:
                missing_techs.append(orig)

        # Если нашли конкретные недостающие технологии — задаём вопрос только по ним
        if missing_techs:
            if len(missing_techs) == 1:
                tech = missing_techs[0]
                q = (
                    f"просим уточнить, есть ли у вас практический опыт работы с {tech}: "
                    f"на каких проектах вы его использовали и какие задачи решали."
                )
            else:
                joined = ", ".join(missing_techs)
                q = (
                    f"просим уточнить ваш практический опыт работы со следующими технологиями: "
                    f"{joined} (проекты, задачи, роль)."
                )
            questions.append(q)
            continue

        # 3) Общие профессиональные требования (без явного списка технологий)
        status_norm = status.lower()

        if "нет (точно нет)" in status_norm or (
            ("нет" in status_norm)
            and ("требует" not in status_norm)
            and ("частично" not in status_norm)
        ):
            # Жёсткий "нет" — подтверждаем отсутствие опыта и даём окно, если опыт есть
            q = (
                "правильно ли мы понимаем, что у вас пока нет опыта по следующему требованию: "
                f"«{requirement}»? Если такой опыт всё же есть, пожалуйста, опишите проекты и задачи."
            )
        else:
            # Частично / Требует уточнения / Не указано и т. п.
            q = (
                "просим подробнее описать ваш опыт по следующему требованию: "
                f"«{requirement}» — на каких проектах вы это применяли, с какими задачами и инструментами работали."
            )

        questions.append(q)

    # Формируем финальный текст блока
    lines = []
    lines.append("ЧТОБЫ ОЦЕНТЬ ВАШЕ ПОЛНОЕ СООТВЕТСТВИЕ ТРЕБОВАНИЯМ ВАКАНСИИ, ПОЖАЛУЙСТА, УТОЧНИТЕ:")
    for q in questions:
        lines.append(f"— {q}")

    lines.append("")
    lines.append("Также просим подтвердить:")
    lines.append("— готовность к выходу после оффера (сроки);")
    lines.append("— формат оформления (штат, контракт, ИП, самозанятый);")

    return "\n".join(lines)


async def generate_mail_for_candidate_utochnenie(
    json_data: dict,
    user_name: str,
    vacancy_text: str,
    group_id: int,
    thread_id: int,
):
    """
    Формирует уточняющее письмо кандидату на основе результатов сверки must_have.

    Логика:
    — На бэкенде строится полный блок вопросов (build_questions_block), с учётом:
        • всех статусов != "Да";
        • анкетных полей;
        • недостающих технологий относительно стека кандидата;
        • общих проф. требований.
    — Модель отвечает только за «рамку» письма (описание проекта/задач/формат и т. п.)
      и должна вставить готовый блок вопросов ДОСЛОВНО.
    """
    try:
        group_id_str = str(group_id).replace("-100", "")
        link = f"https://t.me/c/{group_id_str}/{thread_id}"

        vacancy = json_data.get("vacancy", {})
        vac_id = vacancy.get("position_id", ".")
        candidate = json_data.get("candidate", {})

        must_have_candidate = json_data.get("compliance_check", {}).get("must_have", []) or []

        # Готовый блок уточняющих вопросов
        questions_block = build_questions_block(must_have_candidate, candidate)

        prompt = f"""
Ты — продвинутый ассистент IT-рекрутера компании Omega Solutions.  
Ты должен составить готовое письмо кандидату для уточнения деталей.

---

📌 Основные принципы:
— Пиши строго по структуре ниже, без Markdown, кавычек и пояснений.  
— Используй живой деловой стиль, без излишнего официоза.  
— Структура письма должна быть читаемой и визуально аккуратной.  
— Игнорируй “социальные” качества (ответственность, инициативность и т. п.) — уточняй только технические или фактические моменты.  
— Не пиши ничего о ставках, оплате, бюджете или деньгах.  

---

[ВАКАНСИЯ — СЫРОЙ ТЕКСТ]
{vacancy_text}

ГОТОВЫЙ БЛОК УТОЧНЯЮЩИХ ВОПРОСОВ ДЛЯ КАНДИДАТА (ЕСЛИ НЕ ПУСТОЙ — ВСТАВЬ ЕГО В ПИСЬМО БЕЗ ИЗМЕНЕНИЙ):
{questions_block}

📩 Структура письма:

1️⃣ Приветствие:  
Здравствуйте, {candidate.get('full_name', 'коллега')}!  
(Если есть отчество — используй его. Если нет — только имя и фамилию.)

2️⃣ Благодарность:  
Компания Omega Solutions благодарит вас за предоставленное резюме и интерес к нашим вакансиям. 
Мы рассматриваем вас на позицию {vacancy.get('position_name')} (<a href="https://t.me/c/{group_id_str}/{thread_id}">🆔 {vacancy.get('position_id', '')}</a>) в рамках проекта нашего заказчика.

— Исключи любые упоминания об оплате.
— Не удаляй ссылку с айди из письма.

Описание проекта:
— Извлеки 1–3 предложения ТОЛЬКО из блока [ВАКАНСИЯ — СЫРОЙ ТЕКСТ], разделов типа «О проекте», «Про проект», «Описание проекта». 
— Используй дословные фразы (не перефразируй), при необходимости склей через точку. 
— Если в тексте вакансии нет такого раздела — этот абзац не выводи.

Задачи:
— Извлеки 2–4 строки ТОЛЬКО из разделов «Задачи», «Обязанности», «Что делать». 
— Бери пункты с маркерами (—, -, •) или явные фразы; выводи дословно, по одному пункту в строку (без переформулировок).
— Если раздела с задачами нет — этот абзац не выводи.

Формат работы:
— Укажи формат, если он есть в тексте вакансии (например: удалённо / офис / гибрид).

Особые условия:
— Если присутствуют в тексте (например: NDA, SIM-карта, график, режим занятости) — перечисли одной строкой.
— Любые упоминания про оплату/ставки/зарплату — НЕ выводить.

Технологический стек:
— Перечисли ключевые технологии из [ВАКАНСИЯ — СЫРОЙ ТЕКСТ] и/или из {vacancy.get('tech_stack', [])} одной строкой через запятую, без лишних слов.

Полный перечень требований из запроса (ДОСЛОВНО и ПОЛНОСТЬЮ, без сокращений и объединений, сохраняй исходный порядок; запрещены «…», «и т. д.», «и др.», «etc.»; запрещено объединять строки):

[Обязательные]
— Извлеки КАЖДУЮ исходную строку требований из разделов «Требования:», «Обязательные требования:», «Требуется:», «Must have» и аналогов, а также все пункты с маркерами (—, -, •) под этими заголовками. ОДНА исходная строка = ОДНА строка здесь, без переформулировок и без разбиения.

[Дополнительные]
— Если есть разделы «Будет плюсом:», «Желательно:», «Nice to have», «Опционально:» и т. п., перечисли ДОСЛОВНО КАЖДУЮ строку из этих разделов, в исходном порядке (одна исходная строка = одна строка здесь).

Жёсткие правила по требованиям:
— Даже если список длинный — выводи его ЦЕЛИКОМ. Никаких сокращений и эллипсисов.
— Если в исходной строке встречаются запятые, «/» или «или», всё равно выводи её КАК ЕДИНУЮ строку.
— Если явных заголовков «Обязательные/Дополнительные» нет, строки под «Требования:» трактуй как [Обязательные].
— Если соответствующего раздела нет вовсе — не выводи его заголовок.
— Не атомизируй: если исходная строка содержит перечисления через запятые или «/», «или» — выводи её как один пункт (ровно как в тексте).
— Если соответствующего раздела нет вовсе, этот блок не выводи.

4️⃣ Соответствие кандидата:  
Сравни стек кандидата ({candidate.get('tech_stack', [])}) с требованиями вакансии и подчеркни 2–3 ключевых совпадения.  
Напиши коротко:  
«Ваш опыт с [технологии/области] хорошо соответствует требованиям проекта.»  

5️⃣ Блок уточнений:
— ЕСЛИ ГОТОВЫЙ БЛОК УТОЧНЯЮЩИХ ВОПРОСОВ НЕ ПУСТОЙ, ВСТАВЬ ЕГО В ПИСЬМО ДОСЛОВНО, НИЧЕГО В НЁМ НЕ МЕНЯЯ.  
— ЕСЛИ ГОТОВЫЙ БЛОК УТОЧНЯЮЩИХ ВОПРОСОВ ПУСТ, этот раздел НЕ выводи вовсе.

6️⃣ Если вам интересна данная позиция и у вас есть опыт по перечисленным выше пунктам, пожалуйста, обновите ваше резюме и направьте нам.
Позже мы обязательно свяжемся с Вами,  обсудим детали и удобное для Вас время для прохождения интервью.

7️⃣ Подпись:  
С уважением,  
{user_name}  

---

✍️ Инструкция для модели:
— Составь итоговое письмо строго по структуре.  
— Текст должен быть без пояснений, без маркировки, только готовое письмо.  
— Не добавляй Markdown или комментарии.  
— ОБЯЗАТЕЛЬНО: если блок «ГОТОВЫЙ БЛОК УТОЧНЯЮЩИХ ВОПРОСОВ ДЛЯ КАНДИДАТА» не пустой, вставь его в письмо ДОСЛОВНО, НЕ изменяя ни формулировки, ни количество вопросов.  
— Письмо должно выглядеть как живое деловое сообщение от рекрутера Omega Solutions кандидату.  
— Обязательно включи в письмо реальные детали вакансии (позиция, формат, стек, задачи).  
— Сохрани фирменный стиль: лаконично, чётко, доброжелательно.

Пример письма:
Пример письма уточнения
Здравствуйте, Максим Александрович!

Компания Omega Solutions благодарит вас за предоставленное резюме и интерес к нашим вакансиям.Мы рассматриваем вас на позицию Разработчик-эксперт BPMSoft (<a href="{link}">🆔{vac_id}</a>) в рамках долгосрочного проекта по замещению CRM B2C.

📌 Кратко о вакансии:
— Разработка программных модулей BPMSoft по техническим требованиям;
— Участие в обсуждении архитектуры и требований;
— Интеграции с внешними системами;
— Формат — удалённая полная занятость, поквартальная оплата.

Ваш опыт по BPMSoft, C#, Ext JS и Angular полностью соответствует требованиям вакансии.

ЧТОБЫ ОЦЕНТЬ ВАШЕ ПОЛНОЕ СООТВЕТСТВИЕ ТРЕБОВАНИЯМ ВАКАНСИИ, ПОЖАЛУЙСТА, УТОЧНИТЕ:
— используете ли вы в работе Kafka или аналогичный брокер сообщений?
— работали ли вы с Swagger (OAS) и Postman для тестирования REST API?
— применяете ли вы GitLab в практической разработке?

Также просим подтвердить:
— срок готовности выйти на проект;
— формат оформления (ИП, самозанятый или штат).

Если вам интересна данная позиция и у вас есть опыт по перечисленным выше пунктам, пожалуйста, обновите ваше резюме и направьте нам.
Позже мы обязательно свяжемся с Вами,  обсудим детали и удобное для Вас время для прохождения интервью.

С уважением,
{user_name}
"""

        model = genai.GenerativeModel("gemini-2.5-pro")
        generation_config = genai.GenerationConfig(temperature=0.1)

        response = await model.generate_content_async(
            prompt,
            generation_config=generation_config,
        )
        return response.text.strip()

    except Exception as e:
        print(f"🔥 Ошибка при формировании письма кандидату: {e}")
        return False



async def generate_mail_for_candidate_otkaz(json_data: dict, user_name: str):
  try:
    vacancy = json_data.get('vacancy', {})
    candidate = json_data.get('candidate', {})

    must_have_candidate = json_data.get('compliance_check', {}).get('must_have', [])
    strong_points = []
    for must_have in must_have_candidate:
      if must_have['status'] == 'Да':
        strong_points.append(must_have['requirement'])

    prompt = f"""
Ты — ассистент IT-рекрутера компании Omega Solutions.  
Ты должен составить письмо кандидату с позитивным отказом.

---

📌 **Требования к письму:**
— Пиши строго в деловом, доброжелательном стиле Omega Solutions.  
— Без Markdown, кавычек, комментариев и форматирования.  
— Структура письма должна быть чистой, логичной и вежливой.  
— Не упоминай деньги, ставки, оплату.  
— Используй имя и отчество, если есть в JSON; если нет — имя и фамилию.  

---

📩 **Структура письма:**

1️⃣ **Приветствие:**  
Здравствуйте, {candidate.get('full_name', 'коллега')}!

2️⃣ **Благодарность:**  
Благодарим вас за резюме и интерес к вакансии {vacancy.get('position_name')} (🆔 {vacancy.get('position_id', '')}).  
Было приятно познакомиться с вашим профессиональным опытом.

3️⃣ **Информация об отказе:**  
К сожалению, по итогам рассмотрения профиля мы не можем предложить участие именно в этой вакансии.  
Решение связано с требованиями проекта, а не с уровнем ваших компетенций.

4️⃣ **Акцент на сильных сторонах:**  
Отметьте 2–3 сильные стороны кандидата на основе {strong_points}.  
Пример:  
«Отдельно отмечаем ваш опыт в разработке микросервисов, уверенное владение Kubernetes и участие в архитектурных решениях.»

5️⃣ **Позитивное завершение:**  
Добавь, что компания Omega Solutions сотрудничает с множеством заказчиков и проектов,  
поэтому мы обязательно предложим кандидату другие подходящие вакансии в будущем.  
Напиши в живом стиле:  
«Мы сохраняем ваше резюме в нашей базе и обязательно свяжемся,  
как только появится проект, соответствующий вашему профилю.»

6️⃣ **Подпись:**  
С уважением,  
{user_name}  

---

✍️ **Инструкция для модели:**
— Составь письмо строго по указанной структуре.  
— Не добавляй Markdown, звёздочки, комментарии.  
— Не включай перечисления требований или задач вакансии.  
— Текст должен быть лаконичным, позитивным и вежливым, с акцентом на уважение к кандидату.  
— В конце — только подпись, без лишних слов.

Пример письма:
Здравствуйте, Виктор Сергеевич!

Благодарим вас за предоставленное резюме и интерес к нашим проектам.
Мы внимательно рассмотрели ваш опыт в рамках позиции Архитектор Senior (🆔AR-10032).

По итогам анализа было принято решение не продвигать вашу кандидатуру на данный проект,
так как требовался более выраженный опыт в области архитектурного моделирования (Camunda, C4 Model)
и работы в логистических доменах.

При этом ваш профессиональный бэкграунд — сильный опыт в .NET, Kafka, DevOps и микросервисной архитектуре —
высоко оценивается, и мы уверены, что он будет востребован в других проектах Omega Solutions.

Мы обязательно свяжемся с вами, когда появятся новые вакансии, где ваш опыт будет максимально релевантен.

С уважением,
{user_name}

"""

    model = genai.GenerativeModel('gemini-2.5-pro')
    generation_config = genai.types.GenerationConfig(temperature=0.1)

    response = await model.generate_content_async(prompt, generation_config=generation_config)
    return response.text.strip()

  except Exception as e:
    print(f"🔥 Ошибка при формировании письма кандидату (отказ): {e}")
    return False


async def generate_cover_letter_for_client(json_data: dict, tg_username: str , additional_notes: str = ""):
  try:
    vacancy = json_data.get('vacancy', {}) or {}
    candidate = json_data.get('candidate', {}) or {}
    compliance = json_data.get('compliance_check', {}) or {}

    must_have = compliance.get('must_have', []) or []
    nice_to_have = compliance.get('nice_to_have', []) or []
    

    manager_nick = vacancy.get('manager_telegram_nickname', 'Менеджер')
    full_name = candidate.get('full_name', 'Кандидат')
    position_name = vacancy.get('position_name', 'позиция')
    position_id = vacancy.get('position_id', '')
    grade = candidate.get('grade_and_position', 'грейд уточняется')
    location = (candidate.get('location') or {}).get('city', 'локация уточняется')
    citizenship = (candidate.get('location') or {}).get('citizenship', None)
    stack = ", ".join((candidate.get('tech_stack') or [])[:8]) or 'технологический стек уточняется'
    full_experience = candidate.get('full_experience', 'опыт работы уточняется')

    summary = json_data.get('summary', {}) or {}
    salary = summary.get('salary_expectations', None)
    availability = summary.get('availability', 'ASAP')

    salary_line = f"Ставка: {salary}" if salary and str(salary).strip().lower() != "нужно уточнить" else None
    citizenship_line = f"Гражданство: {citizenship}" if citizenship else None

    # Фильтруем только подтверждённые требования (Да)
    def only_yes(lst):
      out = []
      for x in lst:
        if (x or {}).get('status') == 'Да':
          out.append({
            "requirement": (x or {}).get('requirement', '').strip(),
            "evidence": ((x or {}).get('evidence') or (x or {}).get('notes') or '').strip()
          })
      return out

    must_yes = only_yes(must_have)
    nice_yes = only_yes(nice_to_have)

      
    prompt = f"""
Ты — ассистент IT-рекрутера. Сформируй готовое сопроводительное письмо клиенту СТРОГО по шаблону ниже.
Выводи ТОЛЬКО ЧИСТЫЙ ПЛЕЙН-ТЕКСТ (без Markdown, без кавычек, без маркеров списков, без нумерации).
Сохраняй точные заголовки блоков и порядок строк. Не добавляй лишних строк и разделителей.

ВАЖНО:
В разделе «Требования» НЕ включай формальные поля анкеты: ФИО, Дата рождения, Образование, Грейд, Ставка, Электронная почта/Email.
Показывай только реальные требования вакансии, подтверждённые у кандидата (статус Да).

Правила рендера раздела «Дополнительно подтверждённые требования»:
— На вход подаётся сырой текст (может начинаться с «Дополнительно» или сразу с пунктов), вот он в тройных скобках:
<<<{additional_notes}>>>
— Если после удаления пустых строк и очистки ничего не остаётся, раздел «Дополнительно подтверждённые требования» НЕ выводить вообще.
— Очистка каждой строки:
   1) Обрезать пробелы по краям.
   2) Удалить ТОЛЬКО ведущие буллеты/тире: «—», «-», «•», «*» и последующий пробел.
   3) Схлопнуть двойные пробелы.
   4) Сохранить внутренний текст без изменений, включая «— ✅», «— ⚠️» и т.п.
— Формат раздела при наличии хоть одной строки:
   Дополнительно:
   <первая строка>
   <вторая строка>
   ...
— Между «Дополнительные подтверждённые требования:» и «Итог:» вставлять этот раздел, если он не пуст.
— Не добавляй пустых строк до/после «Дополнительно:».

ШАБЛОН ВЫВОДА (строго следуй форматированию и переносам строк):

Здравствуйте, {manager_nick}
Представляем кандидата {full_name} на позицию {position_name} {'(' + grade + ')' if grade else ''} по запросу 🆔{position_id}.

Краткая информация о кандидате:
Локация: {location}
{citizenship_line or ''}
Грейд: {grade}
{salary_line or ''}
Готов к старту: {availability}
Опыт: {full_experience}
Стек: {stack}

Соответствие требованиям:
Выведи ТОЛЬКО подтверждённые требования (статус Да).
Перед списком выведи отдельной строкой заголовок: Требования:
Далее выводи каждый пункт одной строкой в формате:
<формулировка требования> — ✅ <очень краткое пояснение, если есть>
Не пиши слово «Требование» перед пунктами.
Не используй маркеры и нумерацию.

Обязательные подтверждённые требования:
{must_yes}
{nice_yes }

Дополнительные подтверждённые требования:(Выводи этот блок ТОЛЬКО если есть хоть одна строка в {additional_notes})
{additional_notes.strip()}


Итог:
(Одна-две связные строки. Кратко резюмируй соответствие кандидата позиции и отметь релевантный опыт/домен, готовность к старту и сильные стороны. Укажи позицию и грейд.)

Завершающая строка:
📎 Резюме кандидата прилагается (формат White Label)
С уважением,
{tg_username}/Omega Solutions


ВАЖНЫЕ ПРАВИЛА:
— В блоке "Соответствие требованиям" выводи ТОЛЬКО те требования, где статус Да. Не упоминай и не намекай на остальные.
— Для подтверждённых требований используй маркер ✅.
— Не используй списки и Markdown. Каждый пункт выводи строго в формате одной строки.
— Если пояснения нет — сразу после ✅ ничего не добавляй.
— Если раздел «Дополнительно подтверждённые требования» пуст — не выводи его вообще.
"""

    genai.configure(api_key=os.getenv("GEMINI_API_KEY"))
    model = genai.GenerativeModel("gemini-2.5-pro")
    generation_config = genai.types.GenerationConfig(temperature=0.1)
      
    response = model.generate_content(prompt, generation_config=generation_config)
    return response.text.strip() or ""

  except Exception as e:
    print(f"🔥 Ошибка при формировании письма клиенту: {e}")
    return False




import google.generativeai as genai

async def scrap_vacancy(vacancy_text: str):
    prompt = f"""
Ты — строгий интеллектуальный классификатор вакансий для рекрутингового агентства Omega Solutions.
По входному тексту вакансии ты должна извлечь ключевые параметры и вернуть их в виде ВАЛИДНОГО JSON-объекта БЕЗ лишнего текста.

ОЧЕНЬ ВАЖНО:
— Ответ ДОЛЖЕН быть строго одним JSON-объектом.
— Никакого Markdown, никаких комментариев, никакого текста до или после JSON.
— Используй только двойные кавычки " .
— Не используй запятую после последнего элемента (ни в объектах, ни в массивах).
— Ключи и строки должны быть в двойных кавычках, как в обычном JSON.

Что нужно определить

Из текста вакансии выдели и заполни следующие поля:

"work_format" — формат работы

Допустимые значения:
"remote"      — удалённо
"hybrid"      — гибрид (частично офис, частично удалёнка)
"office"      — только офис
"unspecified" — если формат не указан

"employment_type" — тип работы (формат занятости)

Допустимые значения:
"full_time"   — полный день / фуллтайм / 5/2
"part_time"   — частичная занятость, неполный день
"project"     — проектная работа, занятость на срок проекта, почасовая или по договору подряда
"internship"  — стажировка
"unspecified" — если явно не указано

"english_level" — уровень английского

Допустимые значения:
"A1", "A2", "B1", "B2", "C1", "C2"
"none"        — если явно сказано, что английский не требуется
"unspecified" — если уровень не указан

"grade" — грейд кандидата

Допустимые значения (СТРОГО как в системе):
"Junior"
"Junior+"
"Middle"
"Middle+"
"Senior"
"Lead"
"unspecified"

Интерпретация формулировок:
— "стажёр", "стажер", "intern", "trainee" → "Junior"
— "1–2 года опыта", "Junior" → "Junior"
— "Junior+", "Strong Junior", "Junior strong" → "Junior+"

— "от 3 лет опыта", "Middle", "Middle developer" → "Middle"
— "Middle+", "Strong Middle", "Middle/Senior", "Middle + Senior" → "Middle+"

— "от 5 лет", "Senior", "ведущий специалист", "senior developer" → "Senior"

— "руководство командой", "тимлид", "Team Lead", "Tech Lead",
  "управление разработчиками", "руководитель команды разработки" → "Lead"

— Если формулировки противоречивые или нельзя однозначно отнести к одному из вариантов выше → "unspecified".

"company_type" — тип компании

Допустимые значения:
"startup"
"enterprise"
"product"
"outsourcing"
"outstaff"
"government"
"unspecified"

Теперь главное поле:

"specializations" — специализации (МАССИВ СТРОК НА АНГЛИЙСКОМ ЯЗЫКЕ)

Требования:
— Это основное профессиональное направление позиции.
— ВСЕ значения в массиве "specializations" должны быть написаны ТОЛЬКО на английском (латиницей).
— НЕЛЬЗЯ возвращать названия специализаций на русском языке или с русскими буквами.

Примеры возможных значений (НЕ жёсткий список, а только ориентиры):
"Backend"
"Frontend"
"Fullstack"
"Mobile"
"iOS"
"Android"
"QA"
"QA Automation"
"DevOps"
"Data Engineer"
"Data Scientist"
"Data Analyst"
"System Analyst"
"Business Analyst"
"UI/UX Designer"
"Product Manager"
"Project Manager"
"Solution Architect"
"System Engineer"
"ML Engineer"
и др.

Правила для specializations:
— Если в тексте есть "бэкенд", "бекенд", "backend", "back-end" и т.п. → добавь "Backend".
— Если в тексте есть "фронтенд", "frontend", "front-end" и т.п. → добавь "Frontend".
— Если есть "фулстек", "fullstack", "full-stack" → добавь "Fullstack".
— Для русских формулировок ("Системный аналитик", "Бизнес-аналитик", "инженер по данным") переведи на аккуратный английский:
  например, "System Analyst", "Business Analyst", "Data Engineer".
— НЕ придумывай специализации, которых нет в тексте. Можно только переводить/нормализовывать то, что явно написано.
— Массив "specializations" не должен содержать дублей.
— Если из текста нельзя однозначно понять специализацию → верни пустой массив [].

"skills" — ключевые навыки (массив строк)

Верни массив из основных технологий и навыков, которые ПРЯМО упомянуты в тексте вакансии:

— языки: Java, Python, Go, JavaScript, TypeScript, C#, C++, PHP и т.п.
— фреймворки: Spring, Spring Boot, React, Vue, Angular, Django, FastAPI, .NET, Laravel и т.п.
— базы данных: PostgreSQL, MySQL, Oracle, MS SQL, MongoDB, ClickHouse, Redis и т.п.
— инфраструктура / DevOps: Docker, Kubernetes, K8s, Helm, Terraform, Ansible, CI/CD, GitLab CI, Jenkins, Kafka, RabbitMQ и т.п.
— методологии / подходы: Scrum, Kanban, Agile, TDD, BDD, Microservices и т.п.

Жёсткие правила:
— НЕ ПРИДУМЫВАЙ навыки, которых нет в тексте.
— НЕ добавляй "похожие" или "по смыслу подходящие" технологии.
— Если встречаются синонимы одного и того же (например, "K8s" и "Kubernetes"), выбери один канонический вариант.
— Массив "skills" не должен содержать дублей.

"domains" — сферы / отрасли (массив строк)

Примеры:
"финтех"
"банкинг"
"телеком"
"e-commerce"
"маркетплейс"
"геймдев"
"логистика"
"EdTech"
"HealthTech"
"GovTech"
"HRTech"

Правила:
— Домены можно записывать на том языке, на котором они понятны (русский или английский).
— НЕ придумывай домены, которых нет в тексте.
— Если непонятно — верни [].

"location" — допустимые локации кандидата (массив строк)

Массив стран / регионов, из которых можно рассматривать кандидата.
Примеры значений: "РФ", "РБ", "Казахстан", "СНГ", "Европа", "любая локация", "Глобально".

Интерпретация базовых формулировок:
— "кандидаты из РФ и РБ" → ["РФ", "РБ"]
— "кандидаты из РФ" → ["РФ"]
— "кандидаты из любой точки мира", "гео неважно",
  "любая локация", "any location", "any country",
  "дружественные страны", "friendly countries",
  "без ограничений по географии" → ["Глобально"]

ОСОБЫЕ ПРАВИЛА ДЛЯ СТРОК В МАССИВЕ "location":
— В массив "location" НУЖНО ВЫВОДИТЬ ТОЛЬКО СТРАНЫ ИЛИ КРУПНЫЕ РЕГИОНЫ, БЕЗ ГОРОДОВ.
— Если в тексте указаны и город, и страна (например: "Москва, Россия", "Минск, Беларусь", "Saint Petersburg, Russia"):
  * игнорируй город;
  * в массив "location" добавляй ТОЛЬКО страну в нормализованном виде.
— Нормализация стран:
  * Любая форма "Россия", "РФ", "Russian Federation", "Russia", а также упоминания городов внутри России,
    если понятно, что речь именно о стране (Москва, Санкт-Петербург и т.п.) → добавляй строку "РФ".
  * Любая форма "Беларусь", "Республика Беларусь", "Belarus", а также упоминания городов внутри Беларуси
    (например, "Минск, Беларусь") → добавляй строку "РБ".
— Для других стран:
  * Если страна указана явно (Germany, Германия, Poland, Польша и т.п.) → добавляй ТОЛЬКО название страны на русском языке,
    без города. Например: "Германия", "Польша", "Казахстан".
— Если в тексте встречается только город без страны, и нельзя однозначно понять страну — НЕ добавляй такую локацию в массив.
— Если в вакансии явно перечислены несколько стран/регионов (например: "Россия, Беларусь, Казахстан", "Россия и Европа"):
  * Преобразуй каждую страну по правилам выше, в массиве не должно быть дублей.
— НЕЛЬЗЯ возвращать в "location" значения вида "Москва", "Минск", "Санкт-Петербург" и т.п. — только страны или крупные регионы.
— ЕСЛИ ПОСЛЕ ПРИМЕНЕНИЯ ВСЕХ ПРАВИЛ:
  * в тексте явно сказано, что география не ограничена
    ("любая локация", "any location", "any country", "дружественные страны",
     "friendly countries", "без ограничений по географии", "можно из любой точки мира",
     любые похожие по смыслу формулировки),
    ТОГДА верни "location": ["Глобально"].
  * либо в тексте вообще нет никаких указаний на страны, регионы или ограничения по локации,
    И после всех попыток распознать географию массив остался бы пустым,
    ТОГДА тоже верни "location": ["Глобально"], а НЕ пустой массив.



"manager_username" — логин менеджера (строка)
— Ищи строку вида "Контакт для вопросов: @username".
— В ответе верни только username БЕЗ символа @.
— Если нет логина → "manager_username": "unspecified".

"customer" — заказчик / конечный клиент (строка)
— ВСЕГДА: "RedLab"

"salary" — почасовая ставка для РФ из вакансии (строка)
— Извлеки из текста вакансии ТОЛЬКО почасовую ставку для РФ (Российской Федерации).
— Ищи значения в формате "RUB/час", "руб/час", "RUB/час (Gross)", "руб/час (Gross)" или похожих вариантах.
— Приоритет: ищи в разделе "Месячная ставка для юр лица РФ" или "🇷🇺💰 Месячная ставка для юр лица РФ" в варианте "Выплата ИП/Самозанятый".
— Верни ТОЛЬКО число в виде строки, без валюты, без дополнительных символов, без пробелов, без "(Gross)" и других пометок.
— Пример: если в тексте "1688 RUB/час (Gross)", верни "1688".
— Если почасовая ставка для РФ не указана или не найдена → "salary": "Не указана".
— НЕ бери месячную ставку, НЕ бери ставку для РБ, только почасовая ставка для РФ.

--------------------------------------------------
КАТЕГОРИИ И ПОДКАТЕГОРИИ

Дополнительно из текста вакансии определи общую категорию и подкатегории работы.

"categories" — массив категорий (на русском языке).

Допустимые значения:
"Разработка"
"Тестирование"
"Анализ данных"
"Администрирование"
"Разработка ИИ"

Правила:
— Если позиция связана с созданием программного обеспечения (Backend, Frontend, Fullstack, Mobile, iOS, Android, Desktop, Web и т.п.) → добавь "Разработка".
— Если позиция связана с тестированием (QA, QA Automation, тестировщик, тестирование, SDET и т.п.) → добавь "Тестирование".
— Если позиция связана с анализом данных, ML/AI, статистикой (Data Analyst, Data Engineer, Data Scientist, ML Engineer и т.п.) → добавь "Анализ данных".
— Если позиция связана с администрированием инфраструктуры и систем (System Administrator, DevOps, SRE, System Engineer, Network Engineer и т.п.) → добавь "Администрирование".
— Если позиция прямо связана с машинным обучением, нейросетями, LLM, computer vision, ML Engineer, AI Engineer, Data Scientist, разработкой моделей и решений на основе ИИ → добавь отдельную категорию "Разработка ИИ" (даже если уже добавлена "Разработка" или "Анализ данных").
— Массив "categories" не должен содержать дублей.
— Если по тексту нельзя однозначно отнести позицию ни к одной категории → верни пустой массив [].

"subcategories" — массив подкатегорий (строки).

Правила:
— Подкатегории — это более точный тип работы внутри одной или нескольких категорий.
— Подкатегории нужно подбирать только из жёстко заданного списка (см. ниже).
— НЕ придумывай подкатегории, которых нет в списке.
— Выбирай только те подкатегории, которые действительно соответствуют тексту вакансии.
— Массив "subcategories" не должен содержать дублей.
— Если подкатегории неочевидны → верни [].

ЖЁСТКИЙ СПИСОК ПОДКАТЕГОРИЙ

Для каждой категории "categories" можно использовать только следующие подкатегории в массиве "subcategories":

Для "Разработка":
- "Backend"
- "Frontend"
- "Fullstack"
- "Mobile"
- "Web"

Для "Тестирование":
- "QA Manual"
- "QA Automation"
- "Performance QA"
- "Security QA"
- "QA Lead"

Для "Анализ данных":
- "Data Analyst"
- "Data Engineer"
- "Data Scientist"
- "BI Developer"
- "Analytics Engineer"

Для "Администрирование":
- "System Administrator"
- "System Engineer"
- "Network Engineer"
- "DevOps"
- "SRE"

Для "Разработка ИИ":
- "ML Engineer"
- "AI Engineer"
- "Data Scientist"
- "MLOps"
- "NLP Engineer"

Правила по использованию подкатегорий:
— В "subcategories" допускаются только значения из списка выше.
— Не добавляй другие строки.
— Не добавляй подкатегории, которые не соответствуют тексту вакансии.
— Можно указывать подкатегории сразу из нескольких категорий, если это обосновано текстом (например, "DevOps" и "ML Engineer").
— Массив "subcategories" не должен содержать дублей.

--------------------------------------------------

Общие жёсткие правила:
— Если информация не указана или неочевидна:
  • используй "unspecified" для одиночных полей,
  • используй [] для массивов, кроме поля "location":
    для "location" при полном отсутствии информации или при формулировках про отсутствие ограничений
    НУЖНО возвращать ["Глобально"].
— Строго следуй допустимым значениям для:
  "work_format", "employment_type", "english_level", "grade", "company_type".
— В "specializations" ВСЕГДА используй только английский язык (латиницу), без русских букв.
— НЕ добавляй "похожие" значения только по смыслу, если в тексте нет соответствующих формулировок.

Формат ответа

Ответ ДОЛЖЕН быть строго одним JSON-объектом:

{{
  "work_format": "...",
  "employment_type": "...",
  "english_level": "...",
  "grade": "...",
  "company_type": "...",
  "specializations": [...],
  "skills": [...],
  "domains": [...],
  "location": [...],
  "manager_username": "...",
  "customer": "RedLab",
  "categories": [...],
  "subcategories": [...],
  "salary": "..."
}}

Никакого текста до или после JSON.

Вот текст вакансии:

\"\"\"{vacancy_text}\"\"\""""

    model = genai.GenerativeModel("gemini-2.5-pro")
    generation_config = genai.types.GenerationConfig(
        temperature=0.1,
        response_mime_type="application/json",
    )

    response = await model.generate_content_async(
        prompt,
        generation_config=generation_config,
    )
    return response.text



async def scrap_vacancy_for_new_gr(vacancy_text: str, offer: str):
    prompt = f"""
Ты — строгий интеллектуальный классификатор вакансий для рекрутингового агентства Omega Solutions.
По входному тексту вакансии ты должна извлечь ключевые параметры и вернуть их в виде ВАЛИДНОГО JSON-объекта БЕЗ лишнего текста.

ОЧЕНЬ ВАЖНО:
— Ответ ДОЛЖЕН быть строго одним JSON-объектом.
— Никакого Markdown, никаких комментариев, никакого текста до или после JSON.
— Используй только двойные кавычки " .
— Не используй запятую после последнего элемента (ни в объектах, ни в массивах).
— Ключи и строки должны быть в двойных кавычках, как в обычном JSON.

Что нужно определить

Из текста вакансии выдели и заполни следующие поля:

"vacancy_id" — id вакансии (строка)
"title" — название вакансии (строка)


"work_format" — формат работы

Допустимые значения:
"remote"      — удалённо
"hybrid"      — гибрид (частично офис, частично удалёнка)
"office"      — только офис
"unspecified" — если формат не указан

"employment_type" — тип работы (формат занятости)

Допустимые значения:
"full_time"   — полный день / фуллтайм / 5/2
"part_time"   — частичная занятость, неполный день
"project"     — проектная работа, занятость на срок проекта, почасовая или по договору подряда
"internship"  — стажировка
"unspecified" — если явно не указано

"english_level" — уровень английского

Допустимые значения:
"A1", "A2", "B1", "B2", "C1", "C2"
"none"        — если явно сказано, что английский не требуется
"unspecified" — если уровень не указан

"grade" — грейд кандидата

Допустимые значения (СТРОГО как в системе):
"Junior"
"Junior+"
"Middle"
"Middle+"
"Senior"
"Lead"
"unspecified"

Интерпретация формулировок:
— "стажёр", "стажер", "intern", "trainee" → "Junior"
— "1–2 года опыта", "Junior" → "Junior"
— "Junior+", "Strong Junior", "Junior strong" → "Junior+"

— "от 3 лет опыта", "Middle", "Middle developer" → "Middle"
— "Middle+", "Strong Middle", "Middle/Senior", "Middle + Senior" → "Middle+"

— "от 5 лет", "Senior", "ведущий специалист", "senior developer" → "Senior"

— "руководство командой", "тимлид", "Team Lead", "Tech Lead",
  "управление разработчиками", "руководитель команды разработки" → "Lead"

— Если формулировки противоречивые или нельзя однозначно отнести к одному из вариантов выше → "unspecified".

"company_type" — тип компании

Допустимые значения:
"startup"
"enterprise"
"product"
"outsourcing"
"outstaff"
"government"
"unspecified"

Теперь главное поле:

"specializations" — специализации (МАССИВ СТРОК НА АНГЛИЙСКОМ ЯЗЫКЕ)

Требования:
— Это основное профессиональное направление позиции.
— ВСЕ значения в массиве "specializations" должны быть написаны ТОЛЬКО на английском (латиницей).
— НЕЛЬЗЯ возвращать названия специализаций на русском языке или с русскими буквами.

Примеры возможных значений (НЕ жёсткий список, а только ориентиры):
"Backend"
"Frontend"
"Fullstack"
"Mobile"
"iOS"
"Android"
"QA"
"QA Automation"
"DevOps"
"Data Engineer"
"Data Scientist"
"Data Analyst"
"System Analyst"
"Business Analyst"
"UI/UX Designer"
"Product Manager"
"Project Manager"
"Solution Architect"
"System Engineer"
"ML Engineer"
и др.

Правила для specializations:
— Если в тексте есть "бэкенд", "бекенд", "backend", "back-end" и т.п. → добавь "Backend".
— Если в тексте есть "фронтенд", "frontend", "front-end" и т.п. → добавь "Frontend".
— Если есть "фулстек", "fullstack", "full-stack" → добавь "Fullstack".
— Для русских формулировок ("Системный аналитик", "Бизнес-аналитик", "инженер по данным") переведи на аккуратный английский:
  например, "System Analyst", "Business Analyst", "Data Engineer".
— НЕ придумывай специализации, которых нет в тексте. Можно только переводить/нормализовывать то, что явно написано.
— Массив "specializations" не должен содержать дублей.
— Если из текста нельзя однозначно понять специализацию → верни пустой массив [].

"skills" — ключевые навыки (массив строк)

Верни массив из основных технологий и навыков, которые ПРЯМО упомянуты в тексте вакансии:

— языки: Java, Python, Go, JavaScript, TypeScript, C#, C++, PHP и т.п.
— фреймворки: Spring, Spring Boot, React, Vue, Angular, Django, FastAPI, .NET, Laravel и т.п.
— базы данных: PostgreSQL, MySQL, Oracle, MS SQL, MongoDB, ClickHouse, Redis и т.п.
— инфраструктура / DevOps: Docker, Kubernetes, K8s, Helm, Terraform, Ansible, CI/CD, GitLab CI, Jenkins, Kafka, RabbitMQ и т.п.
— методологии / подходы: Scrum, Kanban, Agile, TDD, BDD, Microservices и т.п.

Жёсткие правила:
— НЕ ПРИДУМЫВАЙ навыки, которых нет в тексте.
— НЕ добавляй "похожие" или "по смыслу подходящие" технологии.
— Если встречаются синонимы одного и того же (например, "K8s" и "Kubernetes"), выбери один канонический вариант.
— Массив "skills" не должен содержать дублей.

"domains" — сферы / отрасли (массив строк)

Примеры:
"финтех"
"банкинг"
"телеком"
"e-commerce"
"маркетплейс"
"геймдев"
"логистика"
"EdTech"
"HealthTech"
"GovTech"
"HRTech"

Правила:
— Домены можно записывать на том языке, на котором они понятны (русский или английский).
— НЕ придумывай домены, которых нет в тексте.
— Если непонятно — верни [].

"location" — допустимые локации кандидата (массив строк)

Массив стран / регионов, из которых можно рассматривать кандидата.
Примеры значений: "РФ", "РБ", "Казахстан", "СНГ", "Европа", "любая локация", "Глобально".

Интерпретация базовых формулировок:
— "кандидаты из РФ и РБ" → ["РФ", "РБ"]
— "кандидаты из РФ" → ["РФ"]
— "кандидаты из любой точки мира", "гео неважно",
  "любая локация", "any location", "any country",
  "дружественные страны", "friendly countries",
  "без ограничений по географии" → ["Глобально"]

ОСОБЫЕ ПРАВИЛА ДЛЯ СТРОК В МАССИВЕ "location":
— В массив "location" НУЖНО ВЫВОДИТЬ ТОЛЬКО СТРАНЫ ИЛИ КРУПНЫЕ РЕГИОНЫ, БЕЗ ГОРОДОВ.
— Если в тексте указаны и город, и страна (например: "Москва, Россия", "Минск, Беларусь", "Saint Petersburg, Russia"):
  * игнорируй город;
  * в массив "location" добавляй ТОЛЬКО страну в нормализованном виде.
— Нормализация стран:
  * Любая форма "Россия", "РФ", "Russian Federation", "Russia", а также упоминания городов внутри России,
    если понятно, что речь именно о стране (Москва, Санкт-Петербург и т.п.) → добавляй строку "РФ".
  * Любая форма "Беларусь", "Республика Беларусь", "Belarus", а также упоминания городов внутри Беларуси
    (например, "Минск, Беларусь") → добавляй строку "РБ".
— Для других стран:
  * Если страна указана явно (Germany, Германия, Poland, Польша и т.п.) → добавляй ТОЛЬКО название страны на русском языке,
    без города. Например: "Германия", "Польша", "Казахстан".
— Если в тексте встречается только город без страны, и нельзя однозначно понять страну — НЕ добавляй такую локацию в массив.
— Если в вакансии явно перечислены несколько стран/регионов (например: "Россия, Беларусь, Казахстан", "Россия и Европа"):
  * Преобразуй каждую страну по правилам выше, в массиве не должно быть дублей.
— НЕЛЬЗЯ возвращать в "location" значения вида "Москва", "Минск", "Санкт-Петербург" и т.п. — только страны или крупные регионы.
— ЕСЛИ ПОСЛЕ ПРИМЕНЕНИЯ ВСЕХ ПРАВИЛ:
  * в тексте явно сказано, что география не ограничена
    ("любая локация", "any location", "any country", "дружественные страны",
     "friendly countries", "без ограничений по географии", "можно из любой точки мира",
     любые похожие по смыслу формулировки),
    ТОГДА верни "location": ["Глобально"].
  * либо в тексте вообще нет никаких указаний на страны, регионы или ограничения по локации,
    И после всех попыток распознать географию массив остался бы пустым,
    ТОГДА тоже верни "location": ["Глобально"], а НЕ пустой массив.



"manager_username" — логин менеджера (строка)
— Ищи строку вида "Контакт для вопросов: @username".
— В ответе верни только username БЕЗ символа @.
— Если нет логина → "manager_username": "unspecified".

"customer" — заказчик / конечный клиент (строка): {offer}


"salary" — почасовая ставка для РФ из вакансии (строка)
— Извлеки из текста вакансии ТОЛЬКО почасовую ставку для РФ (Российской Федерации).
— Ищи значения в формате "RUB/час", "руб/час", "RUB/час (Gross)", "руб/час (Gross)" или похожих вариантах.
— Приоритет: ищи в разделе "Месячная ставка для юр лица РФ" или "🇷🇺💰 Месячная ставка для юр лица РФ" в варианте "Выплата ИП/Самозанятый".
— Верни ТОЛЬКО число в виде строки, без валюты, без дополнительных символов, без пробелов, без "(Gross)" и других пометок.
— Пример: если в тексте "1688 RUB/час (Gross)", верни "1688".
— Если почасовая ставка для РФ не указана или не найдена → "salary": "Не указана".
— НЕ бери месячную ставку, НЕ бери ставку для РБ, только почасовая ставка для РФ.

--------------------------------------------------
КАТЕГОРИИ И ПОДКАТЕГОРИИ

Дополнительно из текста вакансии определи общую категорию и подкатегории работы.

"categories" — массив категорий (на русском языке).

Допустимые значения:
"Разработка"
"Тестирование"
"Анализ данных"
"Администрирование"
"Разработка ИИ"

Правила:
— Если позиция связана с созданием программного обеспечения (Backend, Frontend, Fullstack, Mobile, iOS, Android, Desktop, Web и т.п.) → добавь "Разработка".
— Если позиция связана с тестированием (QA, QA Automation, тестировщик, тестирование, SDET и т.п.) → добавь "Тестирование".
— Если позиция связана с анализом данных, ML/AI, статистикой (Data Analyst, Data Engineer, Data Scientist, ML Engineer и т.п.) → добавь "Анализ данных".
— Если позиция связана с администрированием инфраструктуры и систем (System Administrator, DevOps, SRE, System Engineer, Network Engineer и т.п.) → добавь "Администрирование".
— Если позиция прямо связана с машинным обучением, нейросетями, LLM, computer vision, ML Engineer, AI Engineer, Data Scientist, разработкой моделей и решений на основе ИИ → добавь отдельную категорию "Разработка ИИ" (даже если уже добавлена "Разработка" или "Анализ данных").
— Массив "categories" не должен содержать дублей.
— Если по тексту нельзя однозначно отнести позицию ни к одной категории → верни пустой массив [].

"subcategories" — массив подкатегорий (строки).

Правила:
— Подкатегории — это более точный тип работы внутри одной или нескольких категорий.
— Подкатегории нужно подбирать только из жёстко заданного списка (см. ниже).
— НЕ придумывай подкатегории, которых нет в списке.
— Выбирай только те подкатегории, которые действительно соответствуют тексту вакансии.
— Массив "subcategories" не должен содержать дублей.
— Если подкатегории неочевидны → верни [].

ЖЁСТКИЙ СПИСОК ПОДКАТЕГОРИЙ

Для каждой категории "categories" можно использовать только следующие подкатегории в массиве "subcategories":

Для "Разработка":
- "Backend"
- "Frontend"
- "Fullstack"
- "Mobile"
- "Web"

Для "Тестирование":
- "QA Manual"
- "QA Automation"
- "Performance QA"
- "Security QA"
- "QA Lead"

Для "Анализ данных":
- "Data Analyst"
- "Data Engineer"
- "Data Scientist"
- "BI Developer"
- "Analytics Engineer"

Для "Администрирование":
- "System Administrator"
- "System Engineer"
- "Network Engineer"
- "DevOps"
- "SRE"

Для "Разработка ИИ":
- "ML Engineer"
- "AI Engineer"
- "Data Scientist"
- "MLOps"
- "NLP Engineer"

Правила по использованию подкатегорий:
— В "subcategories" допускаются только значения из списка выше.
— Не добавляй другие строки.
— Не добавляй подкатегории, которые не соответствуют тексту вакансии.
— Можно указывать подкатегории сразу из нескольких категорий, если это обосновано текстом (например, "DevOps" и "ML Engineer").
— Массив "subcategories" не должен содержать дублей.

--------------------------------------------------

Общие жёсткие правила:
— Если информация не указана или неочевидна:
  • используй "unspecified" для одиночных полей,
  • используй [] для массивов, кроме поля "location":
    для "location" при полном отсутствии информации или при формулировках про отсутствие ограничений
    НУЖНО возвращать ["Глобально"].
— Строго следуй допустимым значениям для:
  "work_format", "employment_type", "english_level", "grade", "company_type".
— В "specializations" ВСЕГДА используй только английский язык (латиницу), без русских букв.
— НЕ добавляй "похожие" значения только по смыслу, если в тексте нет соответствующих формулировок.

Формат ответа

Ответ ДОЛЖЕН быть строго одним JSON-объектом:

{{
  "vacancy_id": "...",
  "title": "...",
  "work_format": "...",
  "employment_type": "...",
  "english_level": "...",
  "grade": "...",
  "company_type": "...",
  "specializations": [...],
  "skills": [...],
  "domains": [...],
  "location": [...],
  "manager_username": "...",
  "customer": "{offer}",
  "categories": [...],
  "subcategories": [...],
  "salary": "..."
}}

Никакого текста до или после JSON.

Вот текст вакансии:

\"\"\"{vacancy_text}\"\"\""""

    model = genai.GenerativeModel("gemini-2.5-pro")
    generation_config = genai.types.GenerationConfig(
        temperature=0.1,
        response_mime_type="application/json",
    )

    response = await model.generate_content_async(
        prompt,
        generation_config=generation_config,
    )
    return response.text