#!/usr/bin/env python3

from __future__ import annotations

import ast
import os

from yandex_cloud_ml_sdk import YCloudML
from dotenv import load_dotenv

load_dotenv()

AUTH_TOKEN = os.getenv('AUTH_TOKEN')
FOLDER_ID = os.getenv('FOLDER_ID')


async def del_contacts_gpt(text):
    
    messages = [
        {
            "role": "system",
            "text": """

### **Финальный промт для YandexGPT**

```
Ты — фильтр вакансий. На вход приходит ТЕКСТ ВАКАНСИИ. Работай строго по правилам ниже.
Используй логику И (AND) между разделами. При противоречиях выбирай ОТСЕЧЬ.

Правило приоритета:
- Если обнаружено хотя бы одно запрещающее условие — вакансия ОТСЕКАЕТСЯ, даже если встречаются разрешающие формулировки.

Нормализация эквивалентов:
- РБ ≡ Беларусь ≡ Belarus ≡ BY ≡ в РБ
- РФ ≡ Россия ≡ Russian Federation ≡ Russia ≡ RU ≡ в РФ
- Города РБ: Минск, Брест, Витебск, Гомель, Гродно, Могилёв (любые падежи)
- Ограничители РФ (локация): "только РФ", "из РФ", "на территории РФ", "жители РФ",
  "работа из любой точки РФ", "лок: РФ", "оформление в РФ"
- Ограничители гражданства РФ: "только гражданство РФ", "паспорт РФ обязателен",
  "налоговое резидентство РФ обязательно", "только граждане РФ", "только россияне"


Критерии фильтрации:
* **ВАКАНСИЯ ОТСЕКАЕТСЯ (ВЕРНИ None), если выполняется хотя бы одно из этих условий:**
    * **Локация:** Только РФ/Россия или присутствуют ограничители РФ.
    * **Гражданство:** Только РФ/Россия или присутствуют ограничители гражданства РФ.
    * **Оплата:** Обнаружена отсрочка оплаты (>35 рабочих дней ИЛИ >50 календарных дней) ИЛИ встречаются фразы: "акты поквартально", "квартальное актирование", "поквартальная оплата", "актирование", 'поквартально', 'квартальная оплата', 'поквартальная'.
    * **Текст вакансии** В тексте есть "стоп"/"СТОП".
    * **Дедлайн:** Дата/время подачи резюме в прошлом или сегодня.
    * **Продолжительность проекта:** Указанная продолжительность проекта меньше или равна 3 месяцам (например, "1 мес.", "2 месяца", "3 мес."). Это правило применяется независимо от контекста, например, фраз "(от 3х месяцев)" или "(с возможностью продления)".

* **ВАКАНСИЯ ПРОХОДИТ, если ВСЕ из этих условий выполняются:**
    
    * **Продолжительность проекта:** Указанная продолжительность проекта строго больше 3 месяцев.

**Действия с текстом (выполняй последовательно):**
A) Полностью УДАЛИ из текста упоминания об условиях оплаты.
B) ЗАМЕНИ подписи вида: "Вопросы и предложения ➡️@username или в общий чат. Указать 🆔 запроса." на: "Вопросы и предложения пишите Дмитрию ➡️@Dimitryver. Указать 🆔 запроса." Учти, что @username и формулировки могут отличаться.
C) УДАЛИ все контакты (email, телефон, ссылки, телеграм).
D) Если в начале есть заголовок вакансии (например: "🥇 Java разработчик c Camunda"), УДАЛИ его из текста и верни в ключ "vacancy_title" (без эмодзи и лишних пробелов).
E) Найди первый идентификатор вакансии формата:
   1) "🆔 XX-1234" (XX — латинские заглавные, 1234 — цифры)
   2) "🆔 8581" (только цифры)
   — УДАЛИ его из текста и верни как "vacancy_id" (без символа 🆔). Если нет — None.
F) Найди и обработай ставку или ставку закупки в тексте. Если указаны две ставки — для РФ и для не РФ — возьми ставку для не РФ. Если указана только одна ставка — верни её. Верни только число, без валют, слов и символов (/, ~, от). Если ставки нет ("смотрим вашу" и т.п.) — верни "rate": 0. Удали исходные упоминания ставки из текста.
G) Для требований: Объедини все отдельные секции вроде "➕ Будет плюсом:" или "➕ Будет большим плюсом:" с основными требованиями ("Требования", "Основные требования", "Обязательные требования"). Сохраняй при этом все переносы строк и форматирование. Фразы из "Будет плюсом" должны не выделяться отдельно, а добавляться в конец основного списка требований.
H) Сохрани ВСЕ исходные переносы строк и абзацы, порядок строк, кроме удалённых частей.

Формат ОТВЕТА: строго словарь:
{
  "text": "<очищенный текст вакансии>",
  "rate": "<число или 0>",
  "deadline_date": "DD.MM.YYYY" или None,
  "deadline_time": "HH:MM" или None,
  "vacancy_id": "<id или None>",
  "vacancy_title": "<заголовок или None>"
}
```

"""

        },
        {
            "role": "user",
            "text": text,
        },
    ]

    sdk = YCloudML(
        folder_id=FOLDER_ID,
        auth= AUTH_TOKEN,
    )



    result =(sdk.models.completions("yandexgpt").configure(temperature=0.5).run_deferred(messages, timeout = 180)).wait()
    clean_text = result.alternatives[0].text
    cleaned = clean_text.strip("`\n '")
    print(cleaned)
    cleaned = ast.literal_eval(cleaned)
    print(cleaned)
    print(type(cleaned))
    
    return cleaned



