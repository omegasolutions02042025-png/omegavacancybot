#!/usr/bin/env python3

from __future__ import annotations

import ast
import os
import json
import datetime

from yandex_cloud_ml_sdk import YCloudML
from dotenv import load_dotenv

load_dotenv()

AUTH_TOKEN = os.getenv('AUTH_TOKEN')
FOLDER_ID = os.getenv('FOLDER_ID')

sdk = YCloudML(
    folder_id=FOLDER_ID,
    auth=AUTH_TOKEN,
)

async def check_duration_and_deadline_gpt(text):
    """
    Проверяет текст вакансии и возвращает 'ДА', если ее нужно отсечь по продолжительности
    или дедлайну, и 'НЕТ' в против
    """
    current_date = datetime.date.today().strftime("%d.%m.%Y")
    messages = [
        {
            "role": "system",
            "text": f"""
            Проверь текст вакансии и определи, нужно ли отсечь её. Работай строго по правилам ниже.
            Используй логику ИЛИ (OR) между правилами.

            Правило 1 (Продолжительность): Отсечь, если продолжительность проекта не равна или более 4 месяцам. Если указано более 3 месяцев, то в рамках этого промта будем считать, что это менее 4-х месяцев.
            Примеры для отсева: "Продолжительность проекта (от 3х месяцев): 3 месяца", "Продолжительность проекта (от 3х месяцев): 1 месяц с возможностью продления", "Продолжительность проекта (от 3х месяцев): 2 месяца с возможностью продления", "Продолжительность проекта (от 3х месяцев):1 мес", "Продолжительность проекта (от 3х месяцев):3 мес", "Продолжительность проекта (от 3х месяцев):от 3 мес", "Продолжительность проекта (от 3х месяцев): от 3х месяцев", "3+ мес" и т.п.
            Примеры для пропуска: "Продолжительность проекта (от 3х месяцев):4 мес", "Продолжительность проекта (от 3х месяцев): от 6 мес", "Продолжительность проекта (от 3х месяцев):6+ мес" и т.п.
            Если написано 'от 3х месяцев +' отсекай

            Правило 2 (Дедлайн): Отсечь, если дедлайн в прошлом или сегодня. Сегодняшняя дата: {current_date}.

            Если нужно отсечь хотя бы по одному из правил, верни слово "ДА". Если не нужно отсечь, верни слово "НЕТ". Не добавляй никаких других слов и знаков.
            """
        },
        {
            "role": "user",
            "text": text,
        },
    ]

    try:
        result = (sdk.models.completions("yandexgpt").configure(temperature=0.1).run_deferred(messages, timeout=60)).wait()
        return result.alternatives[0].text.strip()
    except Exception as e:
        print(f"Ошибка при проверке: {e}")
        return "НЕТ"

async def process_bonus_requirements_gpt(text: str) -> str:
    """
    Находит заголовки типа "Будет плюсом" и удаляет только их,
    оставляя текст под ними на месте.
    """
    messages = [
        {
            "role": "system",
            "text": """
            Твоя задача — найти в тексте вакансии определённые заголовки и удалить **только строку с заголовком**, оставив весь последующий текст без изменений.

            1.  **Найди строки**, которые полностью или частично соответствуют одному из следующих заголовков (могут содержать двоеточие или эмодзи ➕):
                * `Будет плюсом`
                * `Плюсом будет`
                * `Дополнительно`
                * `Доп. требования`
                * `Желательно`
                * `Приветствуется`

            2.  **Действие:** Удали **только** найденную строку с заголовком. Не трогай, не перемещай и не удаляй текст, который находится под этим заголовком.

            3.  **ВАЖНОЕ УСЛОВИЕ:** Если в тексте нет ни одного из этих заголовков, верни исходный текст без каких-либо изменений. Сохраняй все переносы строк и абзацы.
            """
        },
        {
            "role": "user",
            "text": text,
        },
    ]
    try:
        result = (sdk.models.completions("yandexgpt").configure(temperature=0.1).run_deferred(messages, timeout=120)).wait()
        return result.alternatives[0].text
    except Exception as e:
        print(f"Ошибка при удалении заголовков 'Будет плюсом': {e}")
        return text # В случае ошибки возвращаем исходный текст    
     # В случае ошибки возвращаем исходный текст
async def del_contacts_gpt(text):
    """
    Основная функция обработки текста вакансии.
    Выполняет очистку, извлечение данных и форматирование.
    """
    messages = [
        {
            "role": "system",
            "text": """
            Ты — фильтр вакансий.
            На вход тебе дается текст вакансии. Твоя задача — проанализировать его, отфильтровать и извлечь данные в формате JSON.

            ### Этап 1: Фильтрация
            Если вакансия не проходит хотя бы по одному из правил ниже → верни None.

            **Локация:**
            * ✅ **Проходит**, если: указано "РБ", "РБ" с другими странами, "Дружественные страны" (включая Беларусь), "Любые", "Без ограничений".
            * ❌ **Отсекается**, если: только "РФ"/"Россия", перечень стран без РБ, или указано "кроме РБ".

            **Гражданство:**
            * ✅ **Проходит**, если: "РБ", "РФ", "Дружественные страны" (включая Беларусь), "Любые", "Без ограничений", "Без разницы".
            * ❌ **Отсекается**, если: указаны страны, исключающие РБ, или "кроме РБ".

            **Условия оплаты:**
            * ❌ **Отсекается**, если: оплата > 35 рабочих дней или > 50 календарных дней, либо есть фразы "акты поквартально", "поквартальное актирование", "квартальная оплата", "актирование".
            * ❌ **Отсекается**, если в тексте есть слово "стоп" или "СТОП".

            ### Этап 2: Извлечение данных и очистка (если вакансия прошла фильтрацию)

            **1. Контакты:**
            * Найди блок "Вопросы и предложения ➡️@username...". Замени его на "Вопросы и предложения пишите Дмитрию ➡️@Dimitryver. Указать 🆔 запроса.".
            * Удали все остальные контакты: Telegram (@...), телефоны, email, ссылки.
            * **ОБЯЗАТЕЛЬНО СОХРАНИТЬ информацию об оформлении (например, "Оформление:", "Оформление в РЛ на минимальную ставку" и т.п.) в тексте.**
            **2. Заголовок (`vacancy_title`):**
            2. Заголовок (`vacancy_title`):
          * Если в начале есть строка-заголовок (например, "🥇 Java разработчик..."), удали её из текста и верни в ключ "vacancy_title" (с сохранением всех символов, включая эмодзи и пробелы). Если нет — None.

            **3. ID Вакансии (`vacancy_id`):**  
            * Найди первый ID формата **"🆔 XX-1234", "🆔 8581", "XX-1234", "8581", "1с 8474" или "1с 1234"**.. Удали его из текста и верни значение в ключ "vacancy_id" без символа 🆔. Удали его из текста и верни значение в ключ "vacancy_id" (без "🆔"). Если нет — None.

            **4. Ставка (`rate`):**
            * Найди ставку. Если есть ставка для РФ и не для РФ, бери для не РФ. Верни только число. Если ставки нет — верни 0.
            * Полностью удали упоминания об условиях оплаты и ставке из текста.
            *Удали упоминания об отстрочке платежа (например, "Отсрочка платежа - 35 рд")
            * **ОБЯЗАТЕЛЬНО СОХРАНИТЬ информацию об оформлении (например, "Оформление:", "Оформление в РЛ на минимальную ставку" и т.п.) в тексте.**

            **5. Дедлайн ('deadline_date', 'deadline_time'):**
            * Найди дедлайн "до DD.MM.YYYY HH:MM". Извлеки дату и время в соответствующие ключи. Если их нет — None.

            **6. Сохранение форматирования:**
            * ❗️ **КЛЮЧЕВОЕ ПРАВИЛО:** В итоговом поле "text" сохрани абсолютно все исходные переносы строк, абзацы и пустые строки, за исключением удаленных блоков. Не меняй порядок строк.

            ### Этап 3: Формат ответа
            Всегда возвращай словарь со всеми ключами. Если данных нет — значение должно быть None.

            {
              "text": "<очищенный текст вакансии с сохраненными абзацами>",
              "rate": "<число или 0>",
              "deadline_date": "DD.MM.YYYY" или None,
              "deadline_time": "HH:MM" или None,
              "vacancy_id": "<id или None>",
              "vacancy_title": "<заголовок или None>"
            }
            """
        },
        {
            "role": "user",
            "text": text,
        },
    ]

    try:
        result = (sdk.models.completions("yandexgpt").configure(temperature=0.1).run_deferred(messages, timeout=180)).wait()
        clean_text = result.alternatives[0].text
        # Добавим `\n` к символам для очистки, чтобы избежать ошибок парсинга
        cleaned = clean_text.strip("`\n '")
        return ast.literal_eval(cleaned)
    except Exception as e:
        print(f"Ошибка при обработке вакансии: {e}")
        return {'text': None, 'rate': None, 'deadline_date': None, 'deadline_time': None, 'vacancy_id': None, 'vacancy_title': None}

async def process_vacancy(text):
    """
    Главная функция, которая координирует проверку и обработку вакансии.
    """
    should_reject = await check_duration_and_deadline_gpt(text)

    if should_reject == 'ДА':
        print("Проект отсеян. Возвращаем None.")
        return {'text': None, 'rate': None, 'deadline_date': None, 'deadline_time': None, 'vacancy_id': None, 'vacancy_title': None}
    else:
        print("Проект подходит. Обрабатываем 'Будет плюсом'...")
        # 1. Сначала обрабатываем необязательные требования
        processed_text = await process_bonus_requirements_gpt(text)

        print("Продолжаем основную обработку.")
        # 2. Затем передаем измененный текст на основную очистку
        return await del_contacts_gpt(processed_text)

# Пример использования (остается без изменений)
async def main():
    vacancy_text = """ 🆔BE-8656

Python developer

Месячная ставка(на руки) до: 185 200 RUB

BE-8656

Локация - РФ, РБ
Оформление в РЛ на минимальную ставку 
Отсрочка платежа - 35 рд

Команда ArenadataDb отвечает за сервис управления аналитической базой данных ArenadataDB.

Перед нами стоит амбициозная цель построить инфраструктуру для удобного, надежного и безопасного управления кластером ArenadataDB.

Именно поэтому мы ищем человека, который готов решать интересные и нетривиальные задачи по стабильной оркестрации IaaS сущностей, увеличению отказоустойчивости и межрегиональному масштабированию сервиса и предоставлению лучшего на рынке сервиса Managed ArenadataDB

Задачи
Мы развиваем и делаем невозможное возможным - запускаем ArenadataDB в облаке:
Внедрять новые фичи автоматизации для клиента недоступные ему при установке ArenadataDB на железо
Подхватывать и внедрять на платформу новые фичи от вендора
Поддерживать высокий темп выпуска новых релизов от вендора Arenadata на облачной платформе

❗️Обязательные данные по кандидату при подаче: ❗️
● ФИО
● Дата рождения
● Страна+Город
● Грейд
● Ставка
● Оценить требования ДА/НЕТ, в соответствии с наличием опыта.️
● ВСЕ ТРЕБОВАНИЯ ИЗ ЗАПРОСА ОТРАЖЕНЫ В ПРОЕКТАХ РЕЗЮМЕ

Требования
Программирование:
Знание принципов ООП, алгоритмов и структур данных;
Опыт промышленной разработки на Python от 3-х лет (разработка API на Flask/FastAPI). Умение писать автоматизированные тесты (pytest);
Базы данных:
Глубокое понимание устройства баз данных;
Знание SQL и умение писать оптимальные запросы для аналитических систем;
Администрирование Linux систем:
Владение UNIX-подобных систем (Centos, Redos);
Понимание концепций виртуализации и контейнеризации. Умение работать с Docker-средой сборки образов;
Непрерывная разработка и интеграция (CI/CD):

Понимание git, git workflows;
Опыт работы с одной из CI/CD систем (Gitlab / GitHub Actions / Teamcity / Jenkins / …);
Опыт работы с системами хранения артефактов (Nexus / Artifactory);

Умеете исследовать и работать с нетривиальными проблемы, а так же работать в команде;
Знакомы с принципами построения распределенных систем;

Работали с облаками (MCS / Aws / OpenStack / … )
Разбираетесь в облачной инфраструктуре, мониторинге, системах управления конфигурациями;
Знакомы с Arenadata решениями для BigData;
Умение писать скрипты автоматизации на bash;
Опыт в администрировании/эксплуатации распределенных аналитических баз данных;

Вопросы и предложения пишите Дмитрию ➡️@Dimitryver. Указать 🆔 запроса.
    """
    result = await process_vacancy(vacancy_text)
    print(result)

# if __name__ == "__main__":
#     import asyncio
#     asyncio.run(main())