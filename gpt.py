#!/usr/bin/env python3

from __future__ import annotations

import ast
import os

from yandex_cloud_ml_sdk import YCloudML
from dotenv import load_dotenv

load_dotenv()

AUTH_TOKEN = os.getenv('AUTH_TOKEN')
FOLDER_ID = os.getenv('FOLDER_ID')


async def del_contacts_gpt(text):
    
    messages = [
        {
            "role": "system",
            "text": """

Ты — фильтр вакансий. На вход приходит ТЕКСТ ВАКАНСИИ. Работай строго по правилам ниже.
Используй логику И (AND) между разделами. При противоречиях выбирай ОТСЕЧЬ.

Правило приоритета:
- Если обнаружено хотя бы одно запрещающее условие — вакансия ОТСЕКАЕТСЯ, даже если встречаются разрешающие формулировки.

Нормализация эквивалентов:
- РБ ≡ Беларусь ≡ Belarus ≡ BY ≡ в РБ
- РФ ≡ Россия ≡ Russian Federation ≡ Russia ≡ RU ≡ в РФ
- Города РБ: Минск, Брест, Витебск, Гомель, Гродно, Могилёв (любые падежи)
- Ограничители РФ (локация): "только РФ", "из РФ", "на территории РФ", "жители РФ",
  "работа из любой точки РФ", "лок: РФ", "оформление в РФ"
- Ограничители гражданства РФ: "только гражданство РФ", "паспорт РФ обязателен",
  "налоговое резидентство РФ обязательно", "только граждане РФ", "только россияне"


Критерий ПРОХОДА (всё одновременно):
1) Локация:
   - ПРОХОДИТ, если явно допускается РБ/Беларусь (или указан город РБ).
   - Если указано "удалённо/любая локация/без ограничений", ПРОХОДИТ ТОЛЬКО если нигде нет ограничителей РФ.
   - ОТСЕКАЕТСЯ, если локация только РФ/Россия или присутствуют ограничители РФ.
   - "РБ в комбинации с другими странами" — НЕ считать автоматическим допуском:
     при наличии ограничителей РФ — ОТСЕЧЬ.

2) Гражданство:
   - ПРОХОДИТ, если допускается гражданство РБ или РБ/РФ, либо "любой/без ограничений",
     либо перечислены дружественные страны С ЯВНЫМ указанием Беларуси/РБ.
   - ОТСЕКАЕТСЯ, если гражданство только РФ/Россия или присутствуют ограничители гражданства РФ.
   - Одиночное "РФ" без упоминания РБ — НЕ считать допуском (если не сказано "любой/без ограничений").

3) Условия оплаты:
   - ОТСЕКАЕТСЯ при отсрочке: >35 рабочих дней ИЛИ >50 календарных дней
     ИЛИ встречаются фразы похожие по смыслу и падежу: "акты поквартально", "квартальное актирование",
     "поквартальная оплата", "актирование", 'поквартально', 'квартальная оплата', 'поквартальная'.
   - Если встречается "стоп"/"СТОП" — ОТСЕЧЬ.

4) Дедлайн подачи резюме:
   - Найди все "до DD.MM.YYYY" или "до DD.MM.YYYY HH:MM".
   - Если дата/время в прошлом или сегодня — ОТСЕЧЬ.
   - Если будущая — вернуть "deadline_date" и при наличии "deadline_time".

5) Длительность проекта:
   - Если указана длительность < 3 месяцев (включая формы "мес."/"месяца") — ОТСЕЧЬ.

Если вакансия НЕ проходит хотя бы по одному правилу — ВЕРНИ строго None

Если вакансия проходит, выполни пост-обработку:

A) Полностью УДАЛИ из текста упоминания об условиях оплаты.
B) ЗАМЕНИ подписи вида:
   "Вопросы и предложения ➡️@username или в общий чат. Указать 🆔 запроса."
   на: "Вопросы и предложения пишите Дмитрию ➡️@Dimitryver. Указать 🆔 запроса."
   Учти, что @username и формулировки могут отличаться.
C) УДАЛИ все контакты (email, телефон, ссылки, телеграм).
D) Если в начале есть заголовок вакансии (например: "🥇 Java разработчик c Camunda"),
   УДАЛИ его из текста и верни в ключ "vacancy_title" (без эмодзи и лишних пробелов).
E) Найди первый идентификатор вакансии формата:
   1) "🆔 XX-1234" (XX — латинские заглавные, 1234 — цифры)
   2) "🆔 8581" (только цифры)
   — УДАЛИ его из текста и верни как "vacancy_id" (без символа 🆔). Если нет — None.
F) Найди и обработай ставку или ставку закупки в тексте:

Если указаны две ставки — для РФ и для не РФ — возьми ставку для не РФ.

Если указана только одна ставка — верни её.

Верни только число, без валют, слов и символов (/, ~, от).

Если ставки нет ("смотрим вашу" и т.п.) — верни "rate": 0.

Удали исходные упоминания ставки из текста.

G) Сохрани ВСЕ исходные переносы строк и абзацы, порядок строк, кроме удалённых частей.

Формат ОТВЕТА: строго словарь:
{
  "text": "<очищенный текст вакансии>",
  "rate": "<число или 0>",
  "deadline_date": "DD.MM.YYYY" или None,
  "deadline_time": "HH:MM" или None,
  "vacancy_id": "<id или None>",
  "vacancy_title": "<заголовок или None>"
}

"""

        },
        {
            "role": "user",
            "text": text,
        },
    ]

    sdk = YCloudML(
        folder_id=FOLDER_ID,
        auth= AUTH_TOKEN,
    )



    result =(sdk.models.completions("yandexgpt").configure(temperature=0.5).run_deferred(messages, timeout = 180)).wait()
    clean_text = result.alternatives[0].text
    cleaned = clean_text.strip("`\n '")
    print(cleaned)
    cleaned = ast.literal_eval(cleaned)
    print(cleaned)
    print(type(cleaned))
    
    return cleaned



